| Función / Bloque                          | Qué hace realmente                                                                                                         | Deuda técnica detectada                                                                                                                                                                           | Severidad |
|-------------------------------------------|----------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| _getAllBacklogItems (CP)                  | Lee backlog de localStorage por proyecto activo                                                                            | Si getActiveProject() devuelve null, usa clave genérica 'backlog-items' — inconsistente con _tplKey() que usan otros módulos. Puede leer el backlog incorrecto.                                  | alta      |
| _cpSearchContext (CP)                     | Busca texto en el raw del CONTEXT del proyecto activo                                                                      | Usa 'context-raw' como key en localStorage; el resto del app usa getProjContext(). Dos fuentes de verdad distintas para el mismo dato. Puede devolver contexto de proyecto equivocado.           | alta      |
| action-search-context comando (CP)        | Navega al sub-tab contexto                                                                                                 | Llama switchTab('backlog') sin prefijo 'tab-' — inconsistente con todos los demás comandos que usan 'tab-backlog'. Si switchTab valida el ID exacto, este comando falla silenciosamente.         | alta      |
| _buildDynamicCommands — proyectos (CP)    | Genera comandos dinámicos por proyecto activo                                                                              | Accede a window.state.projects directamente, y también comprueba getActiveProject(). Doble punto de acceso al mismo estado sin garantía de consistencia.                                         | media     |
| _cpOnListClick (CP)                       | Ejecuta comando al hacer click en resultado                                                                                | Usa parseInt(item.dataset.idx) para indexar _cp.results. Si los resultados cambian entre mouseover y click (búsqueda async futura), el índice puede quedar desincronizado.                      | media     |
| _buildCommandRegistry — action-new-item (CP) | Abre editor de ítem nuevo                                                                                               | setTimeout de 150ms hardcodeado para esperar el cambio de tab antes de abrir el editor. Frágil ante cambios de velocidad de render.                                                              | baja      |
| action-use-template (CP)                  | Navega a backlog y abre template picker                                                                                    | Doble setTimeout anidado (150ms + 120ms) para orquestar navegación + apertura de modal. Race condition potencial si switchTab o openItemEditor son lentos.                                       | baja      |
| _cp.registry (CP)                         | Almacena registro de comandos estáticos                                                                                    | Se regenera en cada apertura con _buildCommandRegistry(). Si el registro es grande, cada apertura tiene costo de reconstrucción. No hay cache entre aperturas.                                   | baja      |
| _mgBuildPlan — emisión de bloque (MG)     | Genera bloque ---PLAN--- (formato legacy)                                                                                  | Emite ---PLAN--- / ---PLAN-END--- en lugar de ---EXECUTION-PLAN--- / ---EXECUTION-PLAN-END--- que define Base Rules §9a. El bloque generado es incompatible con el parser activo de PP.          | alta      |
| _generateContext — stack rewrite (MG)     | Reescribe referencias a Firebase en el stack por Supabase                                                                  | Regex en texto libre puede hacer match en nombres de variables o comentarios legítimos que contengan "Firebase Firestore". Sin test de cobertura visible.                                        | media     |
| _generateContext — commands (MG)          | Construye mapa de arranque por rol desde getAISessions()                                                                   | getAISessions() sin definición visible en este archivo — dependencia implícita de función global. Si falla, el fallback es un placeholder explícito, pero el error se silencia en catch.         | media     |
| _mgInferStatus (MG)                       | Infiere estado operativo del proyecto (closing/active/planning/between_sprints)                                            | Detecta 'closing' comprobando visibilidad del modal #close-sprint-modal por clase, style.display o aria-hidden. Los tres checks son inconsistentes entre sí — uno puede ser true cuando otro es false. | media |
| _mgLoadFiles — deduplicación (MG)         | Carga archivos al dropzone                                                                                                 | Deduplica por nombre exacto únicamente. Si el usuario sube una versión actualizada del mismo archivo, la versión anterior persiste sin warning.                                                   | media     |
| generateDocuments — validación CONTEXT (MG) | Valida campos obligatorios antes de generar                                                                             | Calcula _blItems dos veces en el mismo bloque (AC3) con código idéntico duplicado: una para _valStatus, otra para _valTotal/_valPending. Duplicación sin extracción.                            | baja      |
| _mgExportAllZip (MG)                      | Exporta todos los documentos en ZIP                                                                                        | Referencia a exportFullHistoryMd() está comentada con "descarga directamente — la llamamos en fallback", pero el fallback sí la llama. El comentario contradice el código.                      | baja      |
| confirmMapGenerator — guard sprint cerrado (MG) | Bloquea bump de versión si no hay sprint cerrado                                                                    | showToast de warning se muestra y la función retorna sin descargar nada ni cerrar el modal. El usuario no recibe instrucción clara de qué acción tomar.                                         | baja      |
| _mgApplyBumpedVersion (MG)                | Persiste versión bumpeada y actualiza DOM                                                                                  | El comentario declara "Fuente de verdad única: localStorage key 'app-version-override' declarada en ai-tracker-checkpoint.js". Esta función la escribe directamente con string literal en lugar de usar la constante — acoplamiento implícito entre módulos.  | media |
| _mgNow (MG)                               | Genera timestamp legible                                                                                                   | Usa toLocaleString('es-MX') — el resultado depende del locale del navegador del usuario. En navegadores sin soporte completo de es-MX, el formato puede variar.                                  | baja      |
| _docPrefix (SP)                           | Retorna prefijo del proyecto activo                                                                                        | _PREFIX_MAP contiene 'Obsidiana': 'OB' — hardcodeado con nombre y prefijo legacy. OL-CONTEXT §19 declara que el prefijo fue actualizado a OL. El mapa no refleja el cambio.                     | alta      |
| _generateBacklogMd — filtro generacional (SP) | Filtra ítems para export según sprint activo/cerrado                                                                  | Accede a state.sprints directamente en lugar de getActiveSprints(). En otros puntos del mismo archivo se usa getActiveSprints(). Inconsistencia de acceso al estado de sprints.                  | media     |
| _generateBacklogMd — contadores (SP)      | Calcula contadores máximos de IDs por tipo                                                                                 | ITEMS[i].code[0] asumido siempre válido — si code es null/undefined, code[0] lanza TypeError. No hay guard.                                                                                      | media     |
| _buildCurrentStateMd (SP)                 | Genera bloque ## Estado actual para el backlog exportado                                                                   | Accede a state.sprints directamente. state.sprints y getActiveSprints() pueden devolver datos distintos si hay transformación intermedia. Mismo problema que _generateBacklogMd.                  | media     |
| _generateSprintsExportMd — _itemRow (SP)  | Genera fila de ítem en tabla de sprint                                                                                     | i.code[0] sin guard — mismo riesgo de TypeError que _generateBacklogMd si code es null.                                                                                                          | media     |
| _generateFullHistoryBySprintMd (SP)       | Genera historial completo agrupado por sprint                                                                              | SPRINT_DATA_THRESHOLD hardcodeado en 23 como constante local — no hay campo de configuración ni derivación desde datos de sprints. Cambiar el umbral requiere edición de código.                  | baja      |
| selectProjectFilter / clearProjectFilter (SP) | Cambia filtro activo de proyecto y re-renderiza                                                                       | Ambas funciones llaman la misma secuencia de 7 funciones de render. El bloque no está extraído en una función reutilizable. Duplicación de lógica de refresh.                                    | baja      |
| _renderProjList — archived toggle (SP)    | Toggle de sección archivados en modal de proyectos                                                                         | El handler del toggle usa un string JavaScript inline en onclick con lógica de localStorage. Viola la separación de lógica del HTML y es inauditable desde el código.                             | baja      |
| SplashController.updateProgress (SP)      | Actualiza barra de progreso del splash                                                                                     | Usa style.setProperty('--splash-progress', percent + '%') — CSS custom property. Correcto. Sin deuda.                                                                                           | —         |
| cleanupLocalStorage / testLocalStorageQuota (SP) | Herramientas de debug expuestas en window                                                                          | Funciones de debug expuestas globalmente en producción sin flag de entorno. cleanupLocalStorage() puede ser invocada accidentalmente desde consola por el usuario.                                | baja      |
| Keyboard shortcut Ctrl+K (SP y CP)        | Abre búsqueda global o command palette                                                                                     | Dos listeners de keydown para Ctrl+K en el mismo documento: uno en ai-tracker-sprint-project.js (abre #search-global) y otro en ai-tracker-command-palette.js (abre command palette). Conflict de handlers — el que se registra último gana, o ambos disparan. | alta |
| env.js                                    | Expone SUPABASE_URL y SUPABASE_ANON_KEY en window.__ENV                                                                   | anon key expuesta en texto plano en archivo JS servido al cliente. Esperado para Supabase anon key (solo acceso público), pero el archivo no tiene ningún comentario que documente esto. Sin deuda funcional; riesgo de confusión de seguridad para mantenedores. | baja |