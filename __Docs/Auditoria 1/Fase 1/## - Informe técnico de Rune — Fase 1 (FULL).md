## - Informe técnico de Rune — Fase 1


---
audit-backlog-1a
Archivo leído: ai-tracker-backlog.js — 7866 líneas.


| Función / Bloque | Qué hace realmente | Deuda técnica detectada | Severidad |
|---|---|---|---|
| `ITEMS` (inicialización IIFE, L252–277) | Lee backlog desde localStorage al cargar, con migración inline de status legacy | Migración de status duplicada: ocurre aquí y de nuevo en `loadBacklog()`. Si el proyecto activo no está en localStorage aún, ITEMS arranca vacío silenciosamente sin alerta al usuario. | media |
| `loadBacklog()` (L643–694) | Carga ITEMS desde localStorage o lanza `_loadFromSupabase()`, aplica migraciones, sanea pendientes en sprints cerrados, recalcula prioridades y scores, persiste | `saveBacklog()` se llama al final incondicionalmente, incluso si no hubo migración (`migrated = false`). Escribe al storage en cada carga aunque no haya cambio. | media |
| `_getNextItemCode()` (L805–823) | Genera siguiente código disponible por tipo buscando el NNN máximo en ITEMS con el prefijo del mes actual | Busca solo ítems del mes actual. Si el primer ítem del mes nuevo se crea antes de que haya ítems ese mes, reinicia desde 001 aunque haya IDs previos con NNN alto. Duplicados posibles si dos tabs abren simultáneamente. | alta |
| `parseBacklogMd()` (L826–912) | Parser Markdown de backlog: extrae cada bloque `###` y construye objetos de ítem | `impact` se extrae y se incluye en el objeto pero nunca se usa en ninguna función de render, filtro ni criterio de done del schema. Campo fantasma acumulando datos sin consumo. Regex de `descMatch` puede capturar ruido si hay campos entre `Version:` y `### Criterios`. | baja |
| `importBacklog()` (L962–1077) | Lee archivo .md, parsea, merge con ITEMS en memoria, guarda, re-renderiza | Auto-asignación de tipo de ítem usa `area` como heurística (`area.includes('Bug')`): frágil y no documentado como comportamiento intencionado. `typeChar = 'I'` para tipo desconocido: `'I'` se normaliza a `'P'` en `itemType()`, inconsistencia de intención. Múltiples `console.log` de desarrollo (L969–1029) sin flag de debug. | media |
| `_normalizeStatus()` (referenciada, L857 y L664) | Normaliza valores legacy de status a canónicos | No definida en este archivo — dependencia implícita de módulo externo. Si `_normalizeStatus` no está disponible, `parseBacklogMd` y `loadBacklog` silencian el error (`try/catch` en ITEMS IIFE). | media |
| `_calcPriority()` (L466–477) | Calcula prioridad automática desde tipo, sprint y effort | Regla 3 (`effort 1 + cualquier sprint → high`) se aplica a cualquier sprint incluyendo cerrados, sobreescribiendo la prioridad de ítems en sprints cerrados que deberían ser históricos. | baja |
| `_calcRelevanceScore()` (L490–549) | Score 0–100 para focus mode basado en tipo, sprint, effort, antigüedad, mención en sesión, AC | Llama `getAllSessions()` por cada ítem en cada evaluación. Sin caché. En backlogs grandes con muchas sesiones, esto es O(n×m) en el hot path de render. | alta |
| `_recalcAllScores()` (L552–556) | Estampa `_score` en todos los ITEMS pendientes | Se llama en `setItemStatus()`, `loadBacklog()`, y `toggleBacklogFocusMode()`. Al cambiar el status de un ítem, recalcula scores de TODO el backlog, no solo del ítem afectado. | media |
| `_sanitizePendingInClosedSprints()` (L563–607) | Sanea ítems pendientes en sprints cerrados y corrige doneAt/status inconsistentes | Segunda pasada (doneAt mismatch) silencia la deuda de por qué ocurre: un merge de CHECKPOINT puede sobreescribir status sin respetar doneAt existente. El fix es sintomático, no causal. `B-202604-[pendiente-ID]` documentado como pendiente. | media |
| `_purgeStaleBacklogCache()` (L623–641) | Filtra ITEMS en memoria eliminando done/descartado con >90 días | Muta el array global `ITEMS` directamente (reasignación). No llama `saveBacklog()` ni `_undoSnapshot()` después — la purga no se persiste en el mismo ciclo de llamada ni tiene rollback. Depende de que el caller persista. | alta |
| `_buildOption()` (L2172–2201 y duplicada L2236–2265) | Construye HTML de opción de sprint para el selector | **Función duplicada verbatim**: definida dentro de `_buildSprintSelector()` (L2172) y redefinida dentro de `_blSprintOpen()` (L2236). Cualquier cambio en una copia no se refleja en la otra. | alta |
| `renderHtmlMap()` (L76–241) | Renderiza el Module Map con árbol de módulos, filtros y búsqueda | `loadHtmlMap()` se llama al inicio del render (L80) sin esperar resultado. Si `HTML_MAP_SECTIONS` aún está vacío tras `loadHtmlMap()`, renderiza el empty state incorrectamente. Dependencia implícita de variable global `HTML_MAP_SECTIONS` sin guardia de tipo. | media |
| `_attachBacklogDnD()` (L3624–3669) | Adjunta drag & drop para reordenar ítems dentro de grupos sprint | Solo activa si `backlogSortMode === 'sprint'` (L3625) pero la agrupación por sprint es ahora el modo por defecto — el sort mode fue deprecado (T-202604-424). Condición de activación nunca es `true` con el estado actual del sistema. DnD de reordenamiento silenciosamente inactivo. | alta |
| `mergeBacklogFromTG()` (L4567–4814) | Merge de ítems del TRACKER-GLOBAL a ITEMS: crea, avanza, retrocede, descarta, actualiza | Auto-cierre de P padre (L4769–4778): fuerza `status = 'done'` sin `_undoSnapshot()` previo a esa mutación. En modo `!dryRun`, el P padre queda cerrado sin posibilidad de deshacer esa acción específica. `changed = true` pero el snapshot de undo agrupa todo el merge sin granularidad. | media |
| `_resetStatusSelect()` (L1191–1199) | Resetea visualmente un select de status al valor actual mientras un modal de confirmación está abierto | Usa `getAttribute('onchange').includes("'" + code + "'")` para identificar el select correcto: busca por string en atributo HTML en lugar de data-attribute. Frágil ante cambios de formato del atributo `onchange`. | baja |
| `setItemStatus()` (L1105–1189) | Cambia status de ítem con validaciones, registro en history[], notificaciones de desbloqueo, animación de salida | `_blogLog` (L1175) llamado antes de `_undoSnapshot()` (L1176) — el log queda registrado aunque el usuario haga undo inmediatamente. Log y snapshot fuera de orden. | baja |
| `_buildChildrenBlock()` (L3714–3764) | Construye bloque HTML de hijos de un R con progreso y colapsable | Usa `ITEMS.indexOf(child)` (L3730) como `cIdx` para IDs de DOM (`ibody-${cIdx}`, `ciarrow-${cIdx}`). Si ITEMS muta entre renders (undo, merge), los índices de DOM quedan desfasados con los del array. | alta |
| `buildBacklogItem()` (L3873–~4200) | Construye HTML completo de un ítem del backlog (colapsado + expandido) | Función monolítica de >300 líneas. Mezcla lógica de datos, lógica de estado de UI y generación de HTML. Sin tests posibles de unidad. Cualquier bug en render de ítem requiere leer toda la función. | media |
| `showMergeDiffPanel()` (L4823–~5200) | Muestra panel visual de diff antes de aplicar un merge de CHECKPOINT | `_mdiffToggleSection`, `_mdiffJumpTo`, `_mdiffSetItemSprint`, `_mdiffConfirmNewSprintForm` y `_mdiffCancelNewSprintForm` se asignan como `window.*` dentro de esta función (L5091–~5210). Se redefinen en cada apertura del panel — potencial de leak si el panel se abre múltiples veces. | baja |
| `_scmRender()` (L6412–6459) | Renderiza el modal de cierre de sprint paso a paso | `isLast = step === (skipStep2 ? 3 : 3)` (L6436): condición siempre `3 === 3` independiente de `skipStep2`. La lógica de skipStep2 para el paso final está rota — el último paso siempre es 3 sin variación. | alta |
| `_scmStep1Html()` (L6461–6552) | HTML del paso 1 del modal de cierre: resumen de ítems y métricas de entrega | Accede a `_scmState` directamente como global dentro de una función de render (L6466–6470). `_scmState` se pasa también como parámetro indirectamente vía argumentos. Doble fuente de verdad — si divergen, el render usa la global. | baja |
| `_patchMoreMenuReset` (IIFE, L7794–7824) | Monkey-patch de `toggleMoreMenu` para resetear sub-panel de templates al abrir el menú | Usa polling con `setTimeout(_tryPatch, 100)` hasta que `toggleMoreMenu` esté definido. Sin límite de intentos — loop infinito silencioso si `toggleMoreMenu` nunca se define. | media |
| `_initFocusShortcut` (IIFE, L7832–7846) | Registra shortcut Cmd+F / Ctrl+F para activar Focus Mode solo cuando el tab backlog está activo | Sin mecanismo de cleanup. El listener de `keydown` queda vivo aunque el módulo se reinicialice. Si el archivo se recarga en sesión (hot reload de desarrollo), acumula listeners duplicados. | baja |
| Inline CSS via `style=` en HTML generado (múltiples funciones) | Propiedades de presentación inyectadas como `style=` en strings de HTML (L218, L1948–1951, L2005, L2053–2065, L2195, etc.) | Viola CSS Purity (Base Rules §15). `style.display` no encontrado, pero `style="--var:..."` con propiedades de presentación directas como `width:`, `--sh-bar-w:`, `--bd-w:`, `--mm-bar-w:`, `--kb-type-color:`, `--item-type-color:` mezcladas con CSS custom properties. Las custom properties son permitidas pero los valores calculados se inyectan con literales de presentación inline (ej. L2195: `style="width:${pct}%"`). | media |
| `_blogLog()` (referenciada múltiples puntos) | Registra eventos de backlog en log interno | No definida en este archivo — dependencia implícita de módulo externo sin guardia. Si falla silenciosamente, los logs de auditoría se pierden sin feedback al sistema. | baja |


---
audit-checkpoint-1b

| Función / Bloque                          | Qué hace realmente                                                                                                    | Deuda técnica detectada                                                                                                                                                                          | Severidad |
|-------------------------------------------|-----------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| _effectiveVersion()                       | Lee localStorage override; fallback a APP_VERSION                                                                    | Ninguna                                                                                                                                                                                          | —         |
| _hasStaleSuggestion(ai)                   | Detecta IAs con >3 días sin sesión + ítems en-progreso                                                               | Compara status 'en-progreso' pero el schema canónico usa 'pendiente'. Si el ecosistema migró valores, el filtro nunca dispara                                                                   | alta      |
| AVATAR_LOGOS                              | Banco de SVGs predefinidos por modelo                                                                                 | Ninguna                                                                                                                                                                                          | —         |
| _updateHeaderProjectLabel()               | Renderiza prefijo + nombre canónico del proyecto activo en el header                                                 | El mapa CANONICAL (L55-57) está vacío — declarado pero nunca poblado. El prefijo se deriva con `.slice(0,2)` como fallback, ignorando los prefijos canónicos del ecosistema (OL, AS, CM, AI)   | media     |
| setSyncStatus(status, label)              | Actualiza dot + label de sync en múltiples elementos del DOM                                                         | Accede a _supabaseUser antes de que pueda estar inicializado si se llama muy temprano. No hay guard explícito de orden de inicialización                                                        | baja      |
| handleSyncPillClick()                     | Toggle sign in / sign out                                                                                             | Llama `signInWithSupabase()` como fallback si openAuthModal no existe — dos flujos de auth inconsistentes en el mismo handler                                                                   | media     |
| Supabase init block (L124-184)            | Inicializa cliente, configura auth, suscribe onAuthStateChange                                                        | _supabaseReady resuelve con el primer usuario del callback. Si INITIAL_SESSION dispara antes de SIGNED_IN, la promesa resuelve con null y el caller puede tratar la sesión como inexistente     | alta      |
| getSession() post-listener (L166-177)     | Cubre caso donde INITIAL_SESSION disparó antes del listener                                                          | Si tanto el listener como getSession detectan sesión, `_loadFromSupabase()` y `render()` se llaman dos veces en paralelo. Race condition en carga inicial                                       | alta      |
| _offlineQueuePush(entry)                  | Encola writes pendientes con last-write-wins por type                                                                 | Deduplicación solo por `entry.type` — entradas con `projId` diferente se sobreescriben entre sí (ej: dos proyectos con sessions pendientes, solo el último survives)                           | alta      |
| _offlineQueueFlush()                      | Procesa cola al reconectar                                                                                            | En caso de error parcial, los items fallidos se re-encolan pero el loop ya consumió el queue. Si falla el ítem N, los ítems N+1..M se procesan igual aunque dependan del N                     | media     |
| _refreshMigrationBtnVisibility()          | Oculta botón de migración Firebase → Supabase                                                                        | Función vacía de propósito — el botón ya no existe (AC-8 Fase B). Llamadas desde onAuthStateChange y getSession son dead code ejecutado en cada login                                          | baja      |
| signInWithSupabase()                      | OAuth Google con bifurcación Safari/Chrome                                                                            | La detección Safari por UA string es frágil. Edge, Opera y browsers embedidos pueden pasar el test y recibir redirect cuando necesitarían popup                                                 | media     |
| signOutSupabase()                         | Guarda y cierra sesión                                                                                                | No limpia _realtimeChannel antes del signOut. Si la sesión expira y Supabase emite evento antes del cleanup, el canal puede quedar activo sin usuario                                           | media     |
| openAuthModal / closeAuthModal            | Toggle modal de auth por email                                                                                        | Ninguna                                                                                                                                                                                          | —         |
| signInWithMagicLink()                     | Envía OTP por email                                                                                                   | `shouldCreateUser: true` — crea usuarios nuevos sin restricción de dominio. En un sistema de founder único, cualquier email puede auto-registrarse                                              | alta      |
| _scrollToCard(aiId)                       | Navega al tracker enfocando una card                                                                                  | Función solo resetea scrollTop — ignora el `aiId` recibido. El parámetro no se usa                                                                                                              | baja      |
| switchTab(tab)                            | Cambia tab activo, persiste en localStorage, dispara renders                                                          | Contiene redirect 'hoy' → 'tracker' (L453) pero el tab 'hoy' sigue referenciado en L6698, L4629. Si se elimina el redirect, el tab queda sin contenido sin error explícito                    | media     |
| esc(s)                                    | Escape HTML básico                                                                                                    | No escapa `/` — XSS posible en atributos como href o src si el string escapado se usa en esos contextos                                                                                        | media     |
| _loadTmpIdMap() / _saveTmpIdMap()         | TTL de 24h para tmp-id-map en localStorage                                                                            | `_saveTmpIdMap` hace upsert a Supabase sin await — errores silenciados si el caller no espera. El fallback a _offlineQueuePush está correctamente implementado                                 | baja      |
| _assignPendingIds(tgItems)                | Asigna IDs reales a ítems [pendiente-ID] y [tmp:slug]                                                                 | Busca duplicados por `item.desc` (L596) pero el schema canónico usa `title` — si el ítem llega con `title` y sin `desc`, la detección de duplicados falla silenciosamente                     | alta      |
| showCheckpointPanel(result)               | Renderiza panel de resultado de CHECKPOINT con secciones dinámicas                                                    | `_ckptPendingConfirm` y sus handlers se asignan a `window.*` — se limpian en `_ckptDiffCleanup`, pero si el panel expira por timeout (L975) sin que el usuario confirme, los handlers quedan en window sin cleanup | media     |
| _ckptDiffApplyAll()                       | Aplica retrocesos y descartes confirmados al backlog                                                                  | No emite evento a Supabase directamente — delega a `saveBacklog()`. Si el usuario está offline, los cambios se encolan pero el panel se cierra sin indicación de estado pendiente              | baja      |
| showCheckpointPanel — uso de `i.desc`     | Renderiza descripción de ítems en panel                                                                               | `.slice(0, 60)` sobre `i.desc` (L648, L699, etc) — si el ítem solo tiene `title`, la descripción aparece vacía en el panel                                                                    | media     |
| closeCkptPanel()                          | Cierra panel CHECKPOINT                                                                                               | Ninguna                                                                                                                                                                                          | —         |
| _pauseCkptTimer() / _resumeCkptTimer()    | Pausa/reanuda el timer del panel CHECKPOINT                                                                           | `_ckptTimerEnd` se reinterpreta como "ms restantes" (negativo) en pause y como "timestamp absoluto" en resume. El mismo campo cumple dos semánticas distintas dependiendo del estado           | media     |
| showToast() / _toastRender()              | Sistema de toasts con queue, prioridad y progress bar                                                                 | El regex de detección HTML en `titleHtml` (L1101) es permisivo — `/<[a-z][\s\S]*>/i.test(title)`. Un string con `<3` (menor que 3) puede fallar el esc() y renderizar HTML no escapado        | alta      |
| showToastInline()                         | Toast anclado al elemento que detona la acción                                                                        | En modo acción, el `_outsideHandler` se registra en `document` con `capture:true` pero solo se limpia si el click es fuera del anchor. Si el toast expira por timer, el handler queda activo   | media     |
| save()                                    | Escribe state en localStorage + encola Supabase con debounce                                                         | El cleanup de `ai-tracker-changelog` como fallback de quota no intenta liberar otras claves — solo una. Si el changelog no existe o no es suficiente, la segunda escritura también falla       | baja      |
| _saveFlush()                              | Escritura real en localStorage + Supabase                                                                             | Duplica la lógica de QuotaExceededError de `save()` con mensajes de toast distintos para el mismo error. Inconsistencia en el mensaje al usuario                                               | baja      |
| _saveSessions(proj)                       | Upsert de sesiones en Supabase por lotes de 400                                                                       | Si falla un lote intermedio, hace break y encola el proyecto completo — el re-intento re-envía todos los lotes incluyendo los ya exitosos. Potencial de duplicados en edge cases de red        | media     |
| _blogLog(action, code, detail, doc)       | Log de acciones del backlog en localStorage                                                                           | Ninguna                                                                                                                                                                                          | —         |
| saveBacklog()                             | Escribe ítems y meta del backlog en localStorage + Supabase                                                          | `_localStorageUsageRatio()` y `_purgeStaleBacklogCache()` son llamadas con typeof guard pero nunca están definidas en este archivo — si el archivo que las define no carga, la purga silenciosamente no ocurre | media     |
| saveContextDocs()                         | Escribe context y html-map en Supabase                                                                                | No escribe en localStorage antes de intentar Supabase — a diferencia de saveBacklog. Si Supabase falla y el usuario recarga, los datos recientes de context pueden perderse                   | alta      |
| _migrateV2toV3(raw)                       | Migración automática de state v2 a v3                                                                                 | Usa `Math.random()` para generar el ID del proyecto de migración (L1687). Si la migración se ejecuta dos veces (ej: bug en _isV2State), puede crear proyectos duplicados con IDs distintos    | media     |
| _isV2State(raw)                           | Detecta si el state es formato v2                                                                                     | Si `_stateVersion` es undefined (no 3), y no hay tracker ni sessions, retorna false correctamente. Pero si hay un campo `tracker: {}` vacío, retorna true y dispara migración innecesaria      | media     |
| updateStats()                             | Actualiza badge del sub-tab Tracker con ítems activos                                                                 | Cuenta ítems con `status !== 'done'` — incluye 'descartado' en el conteo de activos                                                                                                            | media     |
| _isInSession(ai)                          | Detecta si una IA tiene sesión en curso                                                                               | Usa `parseInt(b.id)` para ordenar sesiones por ID numérico (L2427). Si los IDs no son numéricos, el sort es incorrecto y puede retornar la sesión equivocada como "última"                    | media     |
| renderStatusBar()                         | Renderiza pill de sprint en tracker-view-header y estado en global footer                                            | El bloque de sprint pill usa `style="--pct:${spPct}%"` directamente en HTML generado (L2464) — violación de CSS Purity                                                                        | alta      |
| _computeNotifications()                   | Computa todas las notificaciones activas del ecosistema                                                               | `getActiveSprints()` se llama con typeof guard pero no está definida en este archivo. Si no carga su módulo, los tipos sprintOrphans y sprintLow no emiten notificaciones sin error visible     | media     |
| hasRecentSession(item, days)              | Determina si un ítem tiene sesión vinculada reciente                                                                  | Si `item.createdAt` es 0 o falsy, la función retorna false y el ítem siempre aparece como inactivo aunque sea nuevo                                                                             | baja      |
| toggleRadarSidebar()                      | Toggle colapso del sidebar Radar                                                                                      | Usa `document.documentElement.style.setProperty` — permitido por CSS Purity. Sin deuda                                                                                                          | —         |
| _initRadarSidebarState()                  | Restaura estado del sidebar desde localStorage al cargar                                                             | El listener de auto-hide (mouseleave/mouseenter) se registra con guard `_rsbAutoHideInited` en window pero dentro de una función que puede llamarse múltiples veces. Si el elemento `sidebar` se recrea en DOM, los listeners apuntan al nodo anterior | media |
| buildCard(ai)                             | Construye el card DOM de una IA                                                                                       | `card.style.setProperty('--card-stagger-delay', '0ms')` en L4487 — permitido por CSS Purity. Sin deuda                                                                                         | —         |
| render()                                  | Renderiza el grid principal del tracker                                                                               | Llama `_initRadarSidebarState()` con guard de window (L4520) pero el guard nunca se resetea si el sidebar se desmonta — el estado puede quedar inconsistente entre renders                     | baja      |
| _buildSessRow(s, isHero)                  | Construye HTML de una fila de sesión                                                                                  | `style="--arr-type-color:..."` en L7160 — violación de CSS Purity en `_showArranquePanel`; la misma constante `_tc` se repite en `_showArranquePanel`, `openPulsoPanel` y otras funciones sin extraer a constante compartida | media |
| avgBetweenSessions(ai)                    | Calcula promedio entre sesiones consecutivas                                                                          | Usa `new Date(s.date)` donde `date` es campo localizado (formato variable). Si el formato cambia, `getTime()` devuelve NaN y se filtra silenciosamente                                         | baja      |
| buildHoyCard(ai)                          | Construye card mini para tab Hoy                                                                                      | Ninguna                                                                                                                                                                                          | —         |
| _cpBacklogItems()                         | Lee ítems del backlog para el Command Palette                                                                         | Lee directamente de localStorage por clave derivada — si el proyecto activo cambia entre llamadas sin invalidar el cache del CP, el palette puede mostrar ítems del proyecto anterior          | baja      |
| _cpRender(query)                          | Renderiza la lista del Command Palette                                                                                 | `list._cpFlat = flat` — almacena referencia al array en el elemento DOM. Si el DOM se recrea, la referencia se pierde y `_cpExecute` falla silenciosamente (flat es null)                     | media     |
| _cpSessionCommands(query)                 | Busca sesiones en todos los proyectos para el palette                                                                 | Ordena por `parseInt(b.s.id || 0)` — misma deuda que _isInSession con IDs no numéricos                                                                                                         | baja      |
| _saveUserPrefs()                          | Guarda shortcuts + templateTrigger + onboardingSeen en Supabase                                                      | No tiene guard de `_supabaseUser` antes de intentar el upsert — el check está dentro del if pero si se llama directamente sin sesión activa, ejecuta la función vacíamente sin señal           | baja      |
| normStatus(raw)                           | Normaliza raw status a alias canónico                                                                                 | Aliases incluyen emojis como parte del valor canónico ('📤 Pendiente'). Si el schema de ITEMS usa 'pendiente' sin emoji (canónico en Base Rules), la normalización produce valores inconsistentes con el backlog real | alta |
| buildTGPreview(items, discrepancy)        | Construye HTML de preview de ítems parseados del CHECKPOINT                                                          | Renderiza `i.desc` (L6507-6508) en lugar de `i.title` — si el ítem solo tiene `title`, la columna descripción aparece vacía                                                                   | media     |
| showMergeDiffPanel()                      | Panel de confirmación antes de aplicar ítems del CHECKPOINT al backlog                                               | `_itemVizKeyHandler` se guarda para limpieza en close, pero el handler de Enter se registra en `document` y solo se limpia en el close explícito — si el panel expira o se cierra por overlay click, puede quedar activo | media |
| _showArranquePanel()                      | Panel de contexto diario al abrir la app                                                                              | Múltiples violaciones de CSS Purity: `style="--arr-type-color:..."` inyectado en innerHTML (L7160, L7201). `style="--pls-vel-fill:..."` y `style="--pls-vel-color:..."` en `openPulsoPanel` (L7573). Consistente con el patrón detectado en renderStatusBar | alta |
| _calcPulsoDotState()                      | Calcula estado del dot de pulso del ecosistema                                                                        | `Math.max(...array.map(...))` en L7216 — si el array de sesiones es grande, spread operator puede causar stack overflow. Sin límite de tamaño                                                 | baja      |
| openPulsoPanel()                          | Renderiza panel de pulso del ecosistema                                                                               | Violación CSS Purity: `style="--pls-vel-fill:...;--pls-vel-color:..."` inyectado en template string (L7573)                                                                                   | alta      |
| _trackerHistDayRender()                   | Renderiza col 1 de vista historial agrupada por día                                                                   | Usa `s.updatedAt || s.createdAt` para filtrar y ordenar. Si ningún campo existe, la sesión queda en 'sin-fecha' pero aun se incluye en el render — puede aparecer en períodos incorrectos     | baja      |
| _trackerRenderMiniHist(aiId)              | Renderiza mini-historial de sesiones de una IA en col 2                                                              | Ninguna deuda estructural                                                                                                                                                                        | —         |
| _trackerRenderHist()                      | Renderiza col 2 con lista de sesiones filtrable por proyecto                                                          | Inline `style` en L7683 (`font-size:10px;color:var(--hint)`) — violación de CSS Purity en HTML generado dinámicamente                                                                         | alta      |
| _trackerHistDragStart/End/AttachDropTargets | Sistema de drag & drop de sesiones hacia textareas                                                                  | `_trackerDropAttached` se marca en el elemento DOM. Si el textarea se destruye y recrea entre renders, el flag se pierde y el listener se registra de nuevo — duplicación de listeners posible | media     |

---

audit-ai-tracker-ai-notes-1c.md

| Función / Bloque                        | Qué hace realmente                                                                                              | Deuda técnica detectada                                                                                                                                                                                                         | Severidad |
|-----------------------------------------|-----------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| toggleNotes(id)                         | Expande/colapsa textarea de notas por IA; alterna texto del toggle                                              | Ninguna                                                                                                                                                                                                                         | —         |
| openAvatarModal(aiId)                   | Abre modal de selección de avatar; genera grid de opciones; pre-selecciona avatar actual                        | Genera innerHTML con onclick inline — viola CSS Purity en el string de template; no usa JS dinámico limpio                                                                                                                      | media     |
| selectAvatarOption(key)                 | Marca opción seleccionada en grid de avatares                                                                   | Usa `event` implícito (global) en lugar de recibir el evento como parámetro — frágil en entornos estrictos; puede romper silenciosamente en modo strict                                                                         | media     |
| confirmAvatarModal()                    | Persiste avatar seleccionado al AI; actualiza popup si está abierto; llama save+render                          | Acoplamiento implícito con variable global `popAIId` — dependencia no declarada en firma                                                                                                                                        | baja      |
| closeAvatarModal()                      | Cierra modal; limpia estado global; restaura foco                                                               | Ninguna                                                                                                                                                                                                                         | —         |
| openAddAI()                             | Abre modal para agregar IA; dispara animación de botón radar                                                    | setTimeout de 50ms para foco — frágil; depende de timing de render                                                                                                                                                             | baja      |
| confirmAddAI()                          | Valida nombre no vacío, detecta duplicados, crea AI con id `ai-${Date.now()}`, guarda y renderiza              | ID generado con `Date.now()` — colisiones posibles en inserciones rápidas (no usa combinación con random); el campo `id` debería venir de PEPE según Base Rules §5                                                              | alta      |
| confirmClear(id)                        | Verifica sesiones existentes antes de mostrar confirm inline                                                    | Ninguna                                                                                                                                                                                                                         | —         |
| deleteAI(id)                            | Confirma eliminación si tiene sesiones; borra directo si no tiene                                               | Verifica sesiones solo en `state.projects` — si hay sesiones legacy en `ai.sessions` (formato v2) la verificación da falso negativo y borra sin confirmar                                                                       | alta      |
| downloadReport(id)                      | Exporta sesiones de una IA como tabla Markdown; mínimo 2 sesiones requerido                                     | Umbral de 2 sesiones no documentado como AC; `getActiveTracker()` — dependencia implícita no declarada en firma                                                                                                                 | baja      |
| downloadGlobalReport()                  | Exporta todas las IAs activas con sesiones en un único Markdown                                                 | Duplica exactamente la lógica de construcción de tabla MD con `downloadReport` — dos fuentes de verdad para el mismo formato de salida                                                                                          | media     |
| toggleCardMenu(id, e)                   | Abre/cierra dropdown ⋯ de card de IA; cierra los demás                                                         | Ninguna                                                                                                                                                                                                                         | —         |
| closeCardMenu(id)                       | Cierra dropdown de card específica                                                                              | Ninguna                                                                                                                                                                                                                         | —         |
| document.addEventListener('click') #1  | Cierra todos los card-dot-dropdown al click global                                                              | Registrado con función anónima — no se puede remover; tres listeners de click globales en el mismo archivo sin coordinación (líneas 218, 292–305)                                                                               | media     |
| archiveAI(id)                           | Toggle de archived en AI; guarda y renderiza                                                                    | Ninguna                                                                                                                                                                                                                         | —         |
| toggleArchivedSection(btn)             | Expande/colapsa sección de IAs archivadas; persiste estado en localStorage                                      | Manipula `btn.textContent` directamente con concatenación — frágil si el texto del botón cambia; viola CSS Purity (manipulación de contenido desde JS que combina UI estática con dinámica)                                     | baja      |
| showInlineConfirm(id, action, msg)      | Crea y adjunta div de confirmación inline a la card                                                             | Usa `esc()` para el mensaje pero el string tiene onclick inline con interpolación directa de `id` y `action` — potencial inyección si `id` o `action` contienen comillas; no usa `_esc()`                                       | alta      |
| closeInlineConfirm(id)                 | Remueve el div de confirmación inline                                                                           | Ninguna                                                                                                                                                                                                                         | —         |
| executeConfirm(id, action)             | Ejecuta acción confirmada (clear o delete); actualiza state y renderiza                                         | `action === 'delete'` no re-verifica que el AI exista antes de mutar `state.ais` — puede correr con card ya eliminada                                                                                                           | baja      |
| closeModal(id)                          | Remueve clase open del modal; restaura foco                                                                     | Ninguna                                                                                                                                                                                                                         | —         |
| document.addEventListener('keydown')   | Escape cierra inline confirms y más-menu y ckpt-panel; Ctrl+Z / Ctrl+Shift+Z dispara undo/redo en sub-tab backlog | Múltiple responsabilidad en un solo listener; Escape no cierra avatar-modal ni otros modales — comportamiento inconsistente entre modales                                                                                        | media     |
| document.addEventListener('click') #2  | Cierra ckpt-panel al click fuera (capture phase)                                                               | Registrado en capture con `true` — puede interferir con otros listeners; no hay cleanup                                                                                                                                         | baja      |
| document.addEventListener('click') #3  | Cierra inline confirms y more-menu al click fuera                                                              | Tercer listener global de click — sin coordinación con los otros dos; puede ejecutar lógica redundante en cada click                                                                                                             | media     |
| toggleMoreMenu()                        | Toggle del dropdown ⋯ global; sincroniza radio buttons de template trigger                                     | Lee estado de `_templateTrigger` con `typeof` check — acoplamiento implícito con función externa no declarada                                                                                                                   | baja      |
| modal-overlay event delegation         | Cierra modales al click en overlay; limpia `popAIId` y `popSessId` para detail-popup                           | Registrado con `querySelectorAll` en tiempo de carga — modales agregados dinámicamente después no tendrán el listener                                                                                                            | media     |
| exportData()                            | Serializa state + documentos de localStorage + ITEMS en memoria a JSON para descarga                           | Lógica de detección de proyecto activo con `_getActiveProjectFilter` — dependencia implícita; proyectos no activos leen backlog de localStorage pero proyectos activos leen de ITEMS en memoria sin garantía de sincronía        | media     |
| _gconfirmOpen / _gconfirmClose / _gconfirmOk | Modal genérico de confirmación con input opcional; callback guardado en closure global | `_gconfirmCb` es global mutable — si se abre dos veces sin cerrar, el primer callback se pierde silenciosamente                                                                                                                  | media     |
| purgeOldSessions / openPurgeModal / closePurgeModal / updatePurgePreview / confirmPurge | Flujo de purgado de sesiones por antigüedad; preview de conteo antes de ejecutar | `confirmPurge` recalcula cutoff desde cero en lugar de reutilizar el calculado en `updatePurgePreview` — posible drift de unos milisegundos entre preview y ejecución real                                                       | baja      |
| toggleBacklogDangerZone()              | Toggle de visibilidad de sección danger del backlog                                                             | Ninguna                                                                                                                                                                                                                         | —         |
| openResetBacklogModal / closeResetBacklogModal / confirmResetBacklog | Flujo de reset del backlog con confirmación por texto "RESET"; limpia ITEMS y localStorage | `ITEMS.length = 0` muta el array por referencia — funciona si todos los módulos comparten la misma referencia, pero es un contrato implícito no documentado; potencial bug si algún módulo hace copia del array                  | media     |
| toggleSidebarDanger()                  | Toggle de clase `open` en danger body del sidebar de templates                                                  | Ninguna                                                                                                                                                                                                                         | —         |
| resetContextData()                     | Abre gconfirm y elimina claves de context de localStorage si se confirma                                       | Ninguna                                                                                                                                                                                                                         | —         |
| resetHtmlMapData()                     | Abre gconfirm y limpia HTML-MAP de localStorage; reasigna `HTML_MAP_SECTIONS = []`                             | `HTML_MAP_SECTIONS = []` reasigna variable global — si hay módulos con referencia al array anterior, la referencia queda stale                                                                                                  | media     |
| _calcPurgeCount(months)                | Cuenta sesiones anteriores a N meses en todos los proyectos                                                    | Ninguna                                                                                                                                                                                                                         | —         |
| importData(e)                          | Lee archivo JSON, valida `d.ais`, pasa a `_showImportDiff`                                                     | Validación mínima — solo verifica existencia de `d.ais`; datos malformados pasan a `confirmImport` sin validación de estructura interna                                                                                         | media     |
| _importCountSessions(d)                | Cuenta sesiones desde `projects[].sessions`                                                                     | Ninguna                                                                                                                                                                                                                         | —         |
| _showImportDiff(d)                     | Calcula diff entre backup y estado actual; renderiza modal de diff                                             | Usa `currentNames` (Set de nombres en minúsculas) para detectar IAs existentes — si dos IAs tienen el mismo nombre en distintas mayúsculas se confunden; no usa `id` como fuente de verdad                                      | media     |
| confirmImport()                        | Merge de IAs, proyectos, sesiones, sprints, tracker items y docs de localStorage                               | Fallback v2 asigna sesiones huérfanas al primer proyecto que tenga sesiones del mismo aiId, o al `mergedProjects[0]` — puede asignar sesiones a proyecto incorrecto silenciosamente; `state = {...state, ais, projects}` — campos no listados en el spread pueden perderse si el estado tiene propiedades no conocidas | alta      |
| _piDetectType(line)                    | Detecta tipo e ID de ítem desde línea de encabezado Markdown                                                   | Tipo por defecto `'T'` cuando no hay código real — puede crear tickets donde debería crear otro tipo                                                                                                                              | baja      |
| _piParseField / _piParseAC             | Parsean campos y criterios de aceptación de bloque Markdown                                                     | `_piParseAC` detecta inicio de AC con `/^###\s+criterios/i` — si el header exacto no coincide, todos los AC se pierden silenciosamente sin aviso al usuario                                                                      | media     |
| piParse()                              | Divide input en bloques por `### ` y parsea cada uno                                                           | `_needsEffortReview` se calcula pero nunca se usa visualmente en el preview (no hay clase ni badge que lo muestre)                                                                                                                | baja      |
| piRenderPreview()                      | Genera HTML del preview de ítems parseados con estado de selección                                             | Genera HTML con `onclick` inline — viola separación JS/HTML; índices `i` usados como selectores de DOM sin garantía de estabilidad si la lista se modifica                                                                       | media     |
| piToggleCard / piToggle / piEditTitle  | Interacciones del preview: expand, selección, edición de título inline                                         | `piEditTitle` muta `_piItems[i].title` directamente sin validación — puede crear ítems con título vacío                                                                                                                          | baja      |
| openItemEditor / closeItemEditor / confirmItemEditor (líneas 794–1462) | Editor completo de ítems del backlog — crear y editar; genera código con `_getNextItemCode` | `_editorItemId` es global mutable compartida entre apertura y cierre — si se abre el editor dos veces sin cerrar, la segunda instancia sobreescribe el contexto de la primera; `_getNextItemCode` depende de función externa no declarada en este archivo | alta      |
| Templates (_PREDEFINED_TEMPLATES, _loadCustomTemplates, _saveCustomTemplates, _getAllTemplates, openTemplatePicker, closeTemplatePicker, _renderTemplatePicker, _applyTemplate, _deleteCustomTemplate, saveCurrentItemAsTemplate, toggleTplSavePanel) | Sistema de templates de ítems: predefinidos y personalizados; picker con delete y aplicación al editor | `_applyTemplate` no tiene guardia ante editor cerrado — si se llama sin editor abierto, escribe en elementos inexistentes silenciosamente; templates personalizados no tienen validación de nombre único                          | baja      |
| getNextOccurrence(resetTime)            | Devuelve próxima Date futura para la hora de reset                                                             | Ninguna                                                                                                                                                                                                                         | —         |
| _resetExpired(resetTime, resetEpoch)   | Verifica si el reset ya ocurrió usando epoch o fallback a hora del día                                         | Fallback legacy compara hora sin fecha — si el usuario deja la app abierta más de 24h con IA exhausted y sin epoch, la liberación es imprecisa                                                                                   | baja      |
| getCD(resetTime, resetEpoch)            | Devuelve string de countdown HH:MM:SS                                                                          | Ninguna                                                                                                                                                                                                                         | —         |
| setInterval countdown (L1738)          | Cada 1s verifica IAs exhausted, las libera si expired, actualiza countdown y stats                             | Llama `render()` completo al liberar cualquier IA — render pesado en loop de 1s puede degradar performance con muchas IAs exhausted liberándose en la misma ventana de tiempo                                                   | media     |
| openTagModal / renderTagPicker / renderColorPicker (parcial) | Sistema de tags: abre modal, renderiza lista de tags con selección y color picker | `renderTagPicker` usa `style="--dot-color:${t.color}"` — CSS custom property inline en elemento; técnicamente no viola CSS Purity (es custom property, no presentación directa) pero es borderline                               | baja      |
| Analytics globals y setters (setCompareProject, setCompareProjectA/B, clearComparison, setAnalyticsPeriod, setAnalyticsRange, setCfProject, setCfType) | Gestión de estado de analytics con variables globales | `setCompareProject` es alias legacy de `setCompareProjectB` — existe por retrocompatibilidad pero no está documentado como deprecated ni tiene aviso; `_analyticsRange` es legacy sin uso real en el render principal             | baja      |
| _getPeriodBounds()                     | Calcula bounds {start,end} del período actual y anterior según `_analyticsPeriod`                              | Ninguna                                                                                                                                                                                                                         | —         |
| _sessInRange(sessions, range)          | Filtra sesiones dentro de un rango con fallback a `_parseSpanishDate`                                          | Ninguna                                                                                                                                                                                                                         | —         |
| _parseSpanishDate(str)                 | Parsea fechas en formato español legacy ("12 abr 2026 11:08 a.m.")                                             | Función de compatibilidad legacy sin fecha de deprecación — acumula dependencia técnica indefinidamente                                                                                                                          | baja      |
| getTooltip / showAnalyticsTooltip / _posTooltip / hideAnalyticsTooltip | Tooltip singleton para gráficos; posicionamiento viewport-aware | `_analyticsTooltip` es singleton global — si se llama antes del DOM estar listo, `document.body.appendChild` puede fallar silenciosamente                                                                                        | baja      |
| _animateCountUp(container)             | Count-up animado en métricas de cards de proyectos; corre solo una vez por flag `_countupDone`                 | `_countupDone` es flag global — si `renderProyectos` se llama en múltiples proyectos o tabs, la animación corre solo en el primero y nunca más; flag nunca se resetea                                                           | media     |
| renderProyectos()                      | Render del dashboard de proyectos con header ecosistema, cards activas y sección archivadas                    | `pct` en `renderProjectAnalytics` referenciado sin estar definido en scope de `barsHtml` (L5919) — variable usada sin declaración, produce NaN en barra de sesiones por mes                                                     | alta      |
| _proyDeleteInline / _proyDeleteExecute / _proyAbrir | Confirmación inline en card de proyecto; eliminación; apertura de proyecto | `_proyAbrir` llama `loadBacklog()` y `loadHtmlMap()` sin await ni callback — si estas funciones son async, el `switchTab` y `switchSubTab` posteriores pueden ejecutarse antes de que los datos estén listos                     | media     |
| _closedItemsInRange / _openedItemsInRange / _closedItemsDetailInRange / _openedItemsDetailInRange | Helpers que leen ítems de localStorage por proyecto y filtran por rango temporal | Leen localStorage en cada llamada sin cache — en `renderAnalytics` estas funciones se llaman múltiples veces en el mismo render; N proyectos × M llamadas = lecturas redundantes de localStorage                                 | media     |
| exportWeeklySummary()                  | Genera resumen semanal en Markdown con sesiones, ítems done/nuevos e insight de horas                          | Nombra las columnas del reporte como "Checkpoints" pero los datos son sesiones — terminología inconsistente con el dominio                                                                                                       | baja      |
| _runDigestToasts()                     | Al abrir app, emite hasta 3 toasts con alertas de proyectos sin sesión e ítems bloqueados                      | Link en toast con `onclick` inline en `innerHTML` — viola separación JS/HTML; `switchTab('tab-proyectos')` usa ID directo que puede no coincidir con el switch real                                                               | media     |
| _buildCumulativeFlowChart()            | SVG de flujo acumulativo (ítems creados vs cerrados) con granularidad adaptativa                               | Lee localStorage en bucle por proyecto sin cache; hardcodea dimensiones W=760, H=220 — no responde a tamaño del contenedor; filtro por tipo usa `startsWith` en code — puede dar falsos positivos si código empieza igual       | media     |
| renderAnalytics() (L3700–4500 aprox)   | Render principal de analytics: KPIs, gráficos, comparación, cycle time, hourly activity                       | Función masiva (estimado >700 líneas) con múltiples subfunciones anidadas y múltiples responsabilidades — imposible testear unitariamente; cycle time tiene un bloque `bySprint` vacío (L4072–4075) con lógica comentada que nunca se ejecuta | alta      |
| _cycleTimeData()                       | Recolecta datos de cycle time por tipo, effort, sprint; detecta outliers                                       | Bloque `bySprint` declarado pero vacío (L4071–4075) — código muerto con comentario placeholder; lee localStorage por proyecto sin cache igual que otros helpers                                                                  | media     |
| renderProductivityPatterns()           | Patrones de productividad por día y hora; efectividad cruzada con ítems cerrados                               | Hora derivada de `s.resetAt` — campo secundario no siempre presente; cuando `resetAt` es null, el registro de hora se pierde silenciosamente; nota en UI advierte esto pero no es suficiente para un dato analítico              | media     |
| renderCheckpointsByProject()            | Tabla de sesiones por proyecto: semana, mes, total con barra proporcional                                      | Inicialización de `projData` duplicada — el loop de "Total" y el helper `_addToProj` hacen el mismo `if (!projData[pid])` con la misma estructura dos veces; código duplicado                                                   | baja      |
| exportAnalyticsMd()                    | Exporta resumen de analytics en Markdown con totales por IA, racha y heatmap                                   | Usa `_analyticsRange` legacy en lugar del sistema de períodos activo — el export no refleja el período seleccionado en la UI                                                                                                    | media     |
| renderProject() (parcial, L5200–5875)  | Render del detalle de proyecto: sesiones, filtros, decisiones, contexto inline                                 | `pct` referenciado sin definición en `renderProjectAnalytics` (mismo bug que renderProyectos); `restoreDrafts` llama `parsePaste(ai.id)` — dependencia implícita en función externa                                               | alta      |
| _qnNavToItem(code)                     | Navega al ítem en backlog y hace scroll; usa setTimeout de 300ms                                               | setTimeout de 300ms para scroll — frágil; si el render tarda más, el scroll no encuentra el elemento                                                                                                                            | baja      |
| _projCtxStartEdit / _projCtxSave / _projCtxCancelEdit | Edición inline del contexto de proyecto en el panel de proyecto | `_projCtxSave` tiene fallback directo a `proj.context = newText; save()` si `setProjContext` no existe — mutación directa de state sin pasar por la capa de persistencia documentada                                             | media     |
| _projCtxToggleSec / _projToggleAIFilter / _projViewSearchInput | Helpers de UI para panel de proyecto | Ninguna deuda significativa                                                                                                                                                                                                     | —         |
| renderProjectAnalytics(projId)         | Analytics inline del proyecto: barras por mes, ranking IAs, racha actual/mejor                                 | `pct` usado en `barsHtml` (L5919) nunca se define en el scope — bug que produce NaN en cada barra; racha actual tiene loop `while(true)` con condición de salida por fecha mínima de 3 meses atrás — puede iterar más de 90 iteraciones innecesariamente | alta      |
| downloadProjectReport(projId)          | Genera reporte Markdown del proyecto con sesiones y decisiones                                                  | Ninguna deuda significativa                                                                                                                                                                                                     | —         |
| restoreDrafts()                        | Restaura drafts de textarea de IAs disponibles desde localStorage                                              | Llama `parsePaste(ai.id)` — dependencia externa implícita; si `parsePaste` no existe en el scope, falla silenciosamente                                                                                                         | baja      |
| currentSubTab / _contextModifiedInSession / _htmlMapModifiedInSession / _backlogModifiedInSession (globals) | Estado de sub-tab activo y flags de modificación en sesión | Cuatro variables globales de estado de sesión sin encapsulación — cualquier módulo puede mutar directamente                                                                                                                      | media     |
| _updateSubTabButtons(sub)              | Muestra/oculta botones de toolbar y danger zone según sub-tab activo                                           | Función de ~80 líneas con múltiple responsabilidad — toolbar, danger zone, contratos, hidden state de plan; difícil de mantener                                                                                                  | media     |
| switchSubTab(sub)                      | Cambia sub-tab activo; dispara renders de cada sub-tab                                                         | Hardcodea la lista de sub-tabs `['backlog','htmlmap','context','plan','contratos']` — si se agrega un sub-tab, hay que actualizarlo en múltiples lugares                                                                         | baja      |
| extractContextSections / mergeContextSections | Extrae y mergea bloques CONTEXT-SECTION del CHECKPOINT al raw del Context | Regex de merge usa `(?=\n## |$)` — si la sección está al final del archivo sin trailing newline, el `$` puede capturar más de lo esperado; merge de versión con regex de string literal frágil                                   | media     |
| extractHtmlMapSections / mergeHtmlMapSections | Extrae y mergea bloques MAP-SECTION del CHECKPOINT al HTML-MAP raw | MAP-SECTION sigue presente como parser activo aunque la decisión 2026-04-27 eliminó MAP-SECTION de los CHECKPOINTs nuevos — código activo para un formato deprecado sin fecha de eliminación                                     | media     |
| renderContext()                        | Render del sub-tab Context: empty state o secciones colapsables                                                | Ninguna deuda significativa                                                                                                                                                                                                     | —         |
| renderContextMd(md)                    | Parsea Markdown básico a HTML para cuerpo de secciones Context                                                 | Listas `<li>` generadas sin wrapper `<ul>/<ol>` — HTML inválido; no maneja listas anidadas                                                                                                                                     | media     |
| renderContextInline(text)              | Renderiza bold e inline code en texto                                                                          | Aplica `esc()` antes de los replace de bold/code — si `esc()` escapa `*` o backticks, los patrones de bold/code no matchean nunca                                                                                              | alta      |
| savePlan / loadPlan                    | Persiste y lee plan por proyecto en localStorage + Supabase async                                              | `loadPlan` soporta legacy (array directo) y nuevo wrapper `{data, _savedAt}` — dos formatos activos sin migración formal; error de Supabase solo loguea a consola + offline queue, sin feedback al usuario                       | baja      |
| renderPlan()                          | Render del sub-tab Plan con scopes sesion/sprint; live status de ítems desde backlog                           | Lee backlog de localStorage en cada render en lugar de usar ITEMS en memoria — puede mostrar datos desincronizados con el estado en memoria; trunca sesión a 3 ítems con mutación del array clonado — el conteo total se recalcula incorrectamente si hay sprints múltiples en scope sesion | media     |
| _buildPulsoPlanesHtml()                | Genera HTML de resumen de planes para panel Pulso                                                              | Lee backlog de localStorage en lugar de ITEMS en memoria (mismo problema que renderPlan)                                                                                                                                        | media     |
| _ctrKey / _ctrLoad / _ctrSave          | Storage helpers para Contratos de Módulo                                                                       | `_ctrSave` captura error con `console.error` — no notifica al usuario si falla el guardado de un contrato                                                                                                                       | baja      |
| _ctrMergeFromItem(itemCode, contract)  | Merge de contrato desde ítem parseado al store de contratos                                                    | No valida que `fn.name` sea único dentro del módulo antes de push — la búsqueda por `find` evita duplicados al editar, pero si dos ítems distintos declaran la misma función con nombres distintos en casing, se crean entradas duplicadas | baja      |
| _ctrUpdateBadge()                      | Actualiza badge de conteos en nav de Contratos                                                                  | Llama `_ctrLoad()` (leer localStorage) solo para contar módulos — deuda de performance menor                                                                                                                                    | baja      |
| onContratosSearch / clearContratosSearch | Búsqueda en Contratos con clear button                                                                        | Ninguna                                                                                                                                                                                                                         | —         |
| _ctrIsRisk(fn)                         | Determina si función fue modificada en últimos 2 sprints activos o recientes                                   | `allSprints.sort()` muta el array original — efecto secundario en el state de proyectos                                                                                                                                         | media     |
| renderContratos()                      | Render principal del sub-tab Contratos: lista de módulos + panel de detalle                                    | Ninguna deuda significativa                                                                                                                                                                                                     | —         |
| openContratoDetail(file)               | Activa módulo en panel de detalle                                                                               | Ninguna                                                                                                                                                                                                                         | —         |
| _esc(s)                                | Escapa HTML para uso en innerHTML                                                                               | Función duplicada — existe `esc()` en otro módulo del ecosistema; dos implementaciones del mismo helper sin consolidación                                                                                                       | media     |
| _renderContratoDetail(mod, el)         | Renderiza panel de detalle de módulo con funciones, invariantes y efectos secundarios                          | Ninguna deuda significativa                                                                                                                                                                                                     | —         |
| exportContratosMd()                    | Exporta todos los contratos como Markdown                                                                       | `showToast('warn', ...)` — tipo `'warn'` probablemente no coincide con el tipo canónico `'warning'` del sistema de toasts; toast silencioso si el tipo no matchea                                                               | baja      |
| resetContratosData()                   | Limpia todos los contratos de localStorage                                                                      | Ninguna                                                                                                                                                                                                                         | —         |
| searchContratos(q)                     | Búsqueda global de contratos integrada con búsqueda unificada                                                  | `switchTab('backlog')` hardcodeado con setTimeout de 80ms — mismo problema de timing que otros navigators                                                                                                                       | baja      |

---

audit-ai-tracker-extra.css-1d

| Función / Bloque                        | Qué hace realmente                                                                                                       | Deuda técnica detectada                                                                                                                                                                                                   | Severidad |
|-----------------------------------------|--------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| Design tokens — :root dark/light        | Define todas las custom properties de color, shadow y severity por tema via data-theme attribute                         | --font-sans y --font-mono NO se declaran en :root. --font-sans solo aparece en el bloque macOS Fidelity Fix (L17214) con !important sobre un selector no-root. body usa var(--font-sans) desde L122 — fallback ausente en non-macOS. --font-mono no declarado en ningún bloque :root del archivo — 25 usos de var(--font-mono) sin fallback confiable | Alta      |
| Global Typography — body                | Font base, color, line-height, antialiasing                                                                              | var(--font-sans) sin declaración :root global — depende de la cascada del macOS Fix block que llega tarde en el archivo y usa !important. En non-Safari el body puede renderizar con font fallback del sistema antes del bloque fix | Alta      |
| [data-theme="dark"] body background     | Gradiente decorativo radial + linear sobre el body dark                                                                  | background-attachment: fixed — causa scroll jank en mobile (fixed attachment no se interpola por GPU en iOS/Android). Sin @media que lo desactive en mobile                                                               | Alta      |
| backdrop-filter — múltiples bloques     | Blur de overlays, sidebars, modales y header                                                                             | De 9 instancias de backdrop-filter: 3 no tienen par -webkit-backdrop-filter (L662 popup-overlay, L3717 quick-note-overlay, L9759 overlay genérico). Safari < 15.4 no aplica el efecto sin prefijo                         | Media     |
| macOS Fidelity block (L17134–17317)     | 7 fixes de fidelidad visual para macOS: backdrop blur, animaciones de modal, font stack, sombras, hover, scrollbars, labels uppercase | Bloque completo usa !important para sobreescribir cascada — 19 declaraciones con !important. --font-sans se declara aquí en lugar de :root global. El bloque no tiene scope: aplica globalmente, no solo en macOS. Mezcla concerns: tipografía, animación, sombras y scrollbars en un solo bloque sin separación por feature | Media     |
| .btn-primary                            | Botón de acción primaria con color de fondo accent                                                                       | color: #0b0c10 hardcoded en L255 y L411 — valor dark-only. En light theme el accent es #6db821 sobre fondo claro — el texto negro funciona, pero el valor no usa var(). Si el accent cambia, el contraste del texto no se actualiza automáticamente | Media     |
| .sync-pill                              | Indicador de estado de sincronización con dot animado y estados ok/syncing/error                                         | Definición duplicada: bloque base en L309 (font, padding, radius, transition) y segundo bloque en L3404 (display, gap, dot via ::before, estados). El primer bloque define font hardcoded ('JetBrains Mono') sin var(). Los estados .synced y .syncing en L5784-5785 usan valores fallback inline distintos al sistema de vars -- inconsistencia de tokens | Media     |
| Breakpoints responsive                  | Media queries para mobile (600px, 560px), tablet (768px, 1024px) y ultrawide (2560px)                                   | Conflicto 900px/899px: max-width:900px (L12031) y max-width:899px (L12760) conviven — gap de 1px sin estilos. Conflicto 600px/601px: min-width:601px (L2286 area) y max-width:600px en 27 bloques — superposición en viewport exacto de 600px. Sin variables CSS para breakpoints: valores hardcoded repetidos en ~45 instancias | Media     |
| Hardcoded hex fuera de :root            | Colores de estado, tipo y UI dispersos en reglas de componente                                                           | 118 instancias de valores hex fuera de :root — no responden a cambios de tema. Incluye colores semánticos de tipo de ítem (.mdiff-card.mdiff-type--*) hardcoded en L18115-18118 duplicando los tokens del sistema de numeración | Media     |
| var() no declaradas — fallbacks sin :root | Bloques de export strip (L17947–17991) usan var(--text-muted), var(--text-secondary), var(--text-primary), var(--border-hover) — 109 instancias | Ninguna de estas vars está declarada en :root dark o light del archivo. Solo tienen fallback rgba hardcoded como segundo argumento. Si otro archivo no las declara, 109 propiedades usan fallback dark-only (rgba blanco) en ambos temas | Media     |
| color-mix() — 161 usos                  | Mezcla de color para fondos semitransparentes de badges, pills, headers y cards de tipo                                  | color-mix(in srgb, ...) no soportado en Safari < 16.2, Firefox < 113, Chrome < 111. Sin @supports wrapper ni fallback. Afecta directamente la legibilidad de badges de tipo, sprint headers y sprint-confirm panels | Media     |
| Bloques con IDs no resueltos            | Estilos de features funcionales (DIFF panel, toolbar redesign, P-item promotion, toast quota, col2 redesign)             | 8 bloques con [tmp:slug] y 10 con [pendiente-ID] — 18 secciones activas en producción sin ID de backlog asignado. Impide trazar qué R/T/B originó cada bloque CSS. R-[tmp:toolbar-backlog-redesign] (L13892–14363) es el bloque más grande sin ID: ~471 líneas | Baja      |
| font-family hardcoded — 102 instancias  | 'JetBrains Mono', monospace directamente en reglas de componente en lugar de var(--font-mono)                            | Si la tipografía monoespaciada cambia, requiere search-replace manual en 102 lugares. var(--font-mono) existe como referencia pero no está declarada en :root — el hardcode es funcional pero no mantenible                | Baja      |
| rgba hardcoded en toast-quota (L18353–18365) | Botones de acción del toast de error de cuota con colores rgba blancos fijos                                        | rgba(255,255,255,0.18/0.10/0.20) son valores dark-only hardcoded. En light theme el botón tiene texto blanco sobre fondo claro del toast — contraste potencialmente inválido                                              | Baja      |
| .rsb-hidden y .hidden utilities         | Clases utilitarias de display:none para toggle de visibilidad vía JS                                                     | .rsb-hidden (L2733) y .hidden (L5673) hacen lo mismo — duplicación de utilidad. .rsb-hidden tiene scope implícito de radar sidebar pero no está scoped por selector padre — aplica globalmente                            | Baja      |

---

audit-css-1e

| Función / Bloque                          | Qué hace realmente                                                                                       | Deuda técnica detectada                                                                                                                                 | Severidad |
|-------------------------------------------|----------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| :root (variables base)                    | Define tokens tipográficos, espaciado, radios, transiciones, z-index, breakpoints y --header-h.          | --bp-ipad, --bp-wide, --bp-ultrawide declaradas en :root pero CSS no las usa en @media — se usan como valores de max-width. No es uso canónico de vars. | baja      |
| :root aliases (--mono, --sans, --font)    | Retrocompatibilidad: redirecciona nombres legacy a los tokens actuales.                                  | 4 aliases de fuente declarados en :root base (no en temas). Si se elimina una variable fuente en el futuro, los aliases no lo detectan.                 | baja      |
| :root[data-theme="dark"] / "light"        | Define paleta semántica por tema: fondos, texto, bordes, colores semánticos, accent, prioridades.        | --amber y --yellow son idénticos en ambos temas (mismos hex). Variable duplicada sin diferenciación. Coste: confusión en uso.                           | baja      |
| Reset global (*, box-sizing)              | Reset universal de box-sizing, margin y padding.                                                         | Sin `html { font-size }` base. La escala tipográfica usa px fijos — no responde a preferencias de zoom del SO sin el media de mobile.                  | media     |
| header (estático)                         | Sticky header con z-index 100, bg y borde inferior.                                                      | z-index hardcodeado en 100 — no usa --z-sticky definido en :root. Inconsistencia con el sistema de z-index declarado.                                  | media     |
| #pepe-splash                              | Pantalla de carga: centra contenido, fade-out con opacity + scale, animación de pulso en imagen.         | --splash-version usa font-size: 10px hardcodeado — no usa token --text-xs. Diverge del sistema de escala.                                              | baja      |
| @keyframes progress-indeterminate         | Anima barra de progreso indeterminada: 0% → 100% width, se queda en 100% el 50–100%.                    | El 50–100% en 100% de width no produce animación de rebote — el efecto visual es incomplete: la barra llega al 100% y se detiene, no cicla correctamente. | media     |
| .tabs / .tab-btn::after (sliding underline) | Tab bar con subrayado animado por ::after que expande width desde 0 a 100%-12px en el tab activo.       | `bottom: -3px` en ::after puede quedar fuera del contenedor si el tab no tiene overflow visible. Depende de contexto del padre.                         | baja      |
| .sync-pill estados                        | Pill de estado sync con variantes synced/offline/local. Solo synced y offline tienen hover definido.     | `.sync-pill.local` hover cambia color de borde y texto pero no background — inconsistente con synced/offline que sí lo cambian.                         | baja      |
| #toast-stack                              | Stack de toasts fixed bottom-right, con gap, max-width responsive, transition en `right`.                | `z-index: 10000` hardcodeado — excede --z-top (9999) definido en :root. El sistema de z-index queda roto en la capa más alta.                          | alta      |
| .toast-item (bloque original)             | Toast base: layout flex, padding, borde, animación enter/exit translateX + scale.                        | Redefinido parcialmente 3 veces en el archivo (líneas ~491, ~6930, ~7064). Propiedades como `align-items`, `transform`, `transition` se sobreescriben en cascada. Orden de especificidad implícita — frágil. | alta      |
| .toast-progress (bloque original)         | Barra de progreso auto-dismiss con scaleX animation y pausa en hover.                                    | Redefinida en B-202604-117 (línea ~7088) con opacity distinta (0.20 vs 0.22) y border-radius vacío. Doble definición activa — la segunda gana por cascada. | media     |
| .toast-dismiss (bloque original)          | Botón dismiss con min-width/height 44px (touch target).                                                  | Redefinido en B-202604-117 (línea ~7136) con min-width/height 28px — reduce el touch target de 44px a 28px. El segundo bloque sobreescribe el primero.  | alta      |
| @media (max-width: 600px) toast           | Mobile: toasts full-width anclados al bottom, sin bordes laterales ni sombra.                            | `.toast-item--inline` en mobile usa `position: static` pero la clase no tiene definición de display o posición base fuera de mobile — comportamiento undefined en desktop. | media     |
| #ckpt-panel                               | Super toast CHECKPOINT: panel fixed bottom-right, 340px, animación opacity+translateY, z-index 9999.    | z-index 9999 hardcodeado — no usa --z-top. Compite con #toast-stack (10000) que lo tapa. Sin variable de control.                                      | media     |
| .ckpt-progress-bar / .ckpt-bar--running   | Barra de progreso del panel CHECKPOINT con animación scaleX.                                             | `.ckpt-progress-bar` declara `animation: none` por defecto. La clase `.ckpt-bar--running` la activa. Si JS no agrega la clase, la barra aparece estática en 100% width — visualmente incorrecta. | media     |
| .avatar-modal                             | Modal de selección de avatar: fixed, inset 0, display:none → display:flex al añadir .open.              | z-index: 1001 hardcodeado — no usa --z-modal (500). Valor aislado que puede quedar por debajo de otros modales con z-index mayor usando el sistema.     | media     |
| #tab-tracker layout                       | Tab tracker: flex-column a 100vh menos header, overflow hidden, max-width ultrawide centrado.            | Usa `var(--header-h, 54px)` como fallback pero --header-h está definido en :root como 53px. El fallback y la variable difieren en 1px.                  | baja      |
| .tracker-detail                           | Panel de detalle: flex:1, padding, overflow-y auto.                                                      | `padding-bottom: 5rem` hardcodeado — sin token. En mobile se sobreescribe a `5rem` de nuevo (idéntico) en el media query — regla duplicada innecesaria.  | baja      |
| .item-body (collapse) / .bitem-body       | Colapsable con max-height: 0 → 2400px y transición ease.                                                 | max-height: 2400px es valor arbitrario. Si el contenido supera 2400px (AC muy largos), la animación se corta y el contenido queda truncado visiblemente durante la transición. | media     |
| .card (multiple redefinitions)            | Card base: background, border, radius, overflow, transition, position relative, box-shadow.              | `.card` tiene 4 bloques de declaración separados a lo largo del archivo (líneas ~1222, ~3573, ~4618, ~4836). `transition` se redefine 3 veces con valores distintos — la última gana. Mantenimiento opaco. | alta      |
| .card.focus-active .card-footer           | En focus mode, footer sticky con z-index 2, bg y sombra.                                                 | z-index: 2 puede quedar por debajo de elementos posicionados dentro del card que tengan z-index implícito. Sin contexto stacking claro.                 | baja      |
| .card.focus-active / .focus-exit-btn      | `.focus-exit-btn` definido dos veces: una en el bloque original (~línea 3539) y otra en FASE 2 (~línea 4882). Propiedades divergen: border-radius usa --radius-pill en uno, pill literal en otro (ambos son pill). | Segunda definición duplica y sobreescribe. Bloque original huérfano. | media     |
| .card-drag-handle                         | Handle de drag con opacity 0 → 1 en hover del card.                                                     | Definido dos veces (~línea 3496 y ~línea 4898). Propiedades difieren: font-size 12px vs 14px. El segundo bloque (FASE 2) gana — el primero es código muerto. | media     |
| .interrupted-banner                       | Banner de sesión interrumpida: display:none → flex, con animación bannerSlideIn.                         | Definido dos veces: línea ~1617 y línea ~4853. Propiedades background y border-bottom difieren (rgba values distintos para el mismo color amber). El segundo sobreescribe al primero. | media     |
| .hoy-stats-bar                            | Stats bar en Tab Hoy: flex con gap:0 declarado dos veces en la misma regla (línea ~1657).                | `gap: 0` duplicado dentro del mismo selector — propiedad repetida en una sola regla CSS. Residuo de edición.                                            | baja      |
| .hoy-exh-countdown.soon                   | Countdown con color amber y text-shadow usando color-mix().                                              | `color-mix(in srgb, var(--amber) 45%, transparent)` — color-mix no tiene fallback. Si el browser no soporta color-mix, el text-shadow no renderiza.    | media     |
| tc-0 a tc-7 (tags)                        | 8 clases de color de tags con valores hex hardcodeados para border-color.                                | Los hex de border-color (#3a3278, #153828, etc.) no son variables del sistema de tokens. No responden a theme switching — solo están definidos en dark implícito. | alta      |
| .tg-overlay / .pend-overlay               | Overlays de paneles laterales: fixed inset, z-index 250.                                                 | z-index 250 — entre --z-sidebar (200) y --z-dropdown (400). No usa el sistema de tokens de z-index.                                                    | media     |
| .more-menu                                | Dropdown de acciones extra: z-index 300.                                                                 | z-index 300 — no usa --z-dropdown (400). Puede quedar por debajo de otros dropdowns en contextos anidados.                                              | media     |
| .popup-overlay / .modal-overlay           | Overlays de modales: z-index 200 / 100.                                                                  | z-index 200 y 100 hardcodeados. --z-modal está en 500 — todos los modales del sistema están por debajo de su propio token declarado.                    | alta      |
| .onboarding-overlay                       | Overlay de onboarding: z-index 600.                                                                      | z-index 600 — supera --z-modal (500) y --z-overlay (900)? No. Pero no usa el token --z-overlay. Fuera del sistema.                                     | media     |
| .fb-onboarding-overlay                    | Overlay wizard Firebase: z-index 500.                                                                    | Coincide con --z-modal numéricamente pero no usa la variable. Si --z-modal cambia, este overlay no se actualiza.                                        | baja      |
| .status-confirm-overlay                   | Confirmación de retroceso/descarte: z-index 9999.                                                        | z-index 9999 hardcodeado — compite con #ckpt-panel (9999) y está por debajo de #toast-stack (10000). Orden de apilamiento determinado por DOM, no por sistema. | media     |
| .promote-modal-overlay                    | Modal de promoción de ítem: z-index 100.                                                                 | z-index 100 — menor que --z-sidebar (200). Puede quedar tapado por el sidebar en contextos donde ambos coexisten.                                       | media     |
| .proj-modal-overlay                       | Modal de gestión de proyectos: z-index 300.                                                              | z-index 300 — no usa --z-modal (500). Inconsistente con gconfirm-overlay (500) y purge-modal-overlay (400). Tres modales con tres z-index distintos sin sistema. | alta      |
| .gconfirm-overlay                         | Modal genérico de confirmación: z-index 500.                                                             | Único modal que usa el valor de --z-modal pero sin la variable. Si el token cambia, no se actualiza.                                                    | baja      |
| .purge-modal-overlay / .import-diff-overlay | Modales de purge e import diff: z-index 400 cada uno.                                                | z-index 400 — entre dropdown (400) y modal (500). Colisión potencial con dropdowns abiertos durante estos modales.                                      | media     |
| .item-detail-panel                        | Panel lateral de detalle de ítem: sticky, max-height, width animado 0 → 340px.                          | `height: calc(100vh - 5rem)` en .open — no usa --header-h. Si el header cambia de altura, el panel no se actualiza.                                    | media     |
| @media (max-width: 600px) #grid           | Grid de 1000px–601px: 2 columnas.                                                                        | Selector `@media (max-width: 1000px) and (min-width: 601px)` no usa --bp-ipad (1024px) — breakpoint hardcodeado discrepante con el token declarado.    | media     |
| .context-raw-block                        | Bloque de texto raw de contexto: font-family con string literal 'JetBrains Mono'.                        | Usa string literal en lugar de `var(--font-mono)`. Si la fuente mono cambia, este bloque no se actualiza.                                              | baja      |
| .heatmap-cell hover                       | Hover en celda heatmap: `opacity: 0.72 !important`.                                                      | `!important` en hover — fuerza cascade. Señal de especificidad mal estructurada. Dificulta sobreescribir en variantes o estados especiales.             | media     |
| .proy2-card animations                    | Animación de entrada de cards de proyectos con stagger via `--card-idx`.                                 | La animación `proy2CardIn` se declara tres veces para `.proy2-card` (base, selected, flash) en la misma regla compuesta. El stagger `animation-delay` sobreescribe la animación de `.proy2-selected` que ya tiene dos animations. Resultado visual puede ser incorrecto. | media |
| @keyframes cardFlash                      | Flash de card al guardar (v1): box-shadow pulse verde.                                                   | Definida pero marcada en comentario como "reemplazada por cardFlashPremium". Keyframe huérfano — código muerto que permanece en el bundle.              | baja      |
| .radar-stats-grid / .radar-card           | Grid de stats del Radar en Tab Hoy — bloque con indentación de 2 espacios extra en todo el bloque.       | Bloque completo (~líneas 5241–5321) tiene indentación irregular (2 espacios de más). No es error funcional pero señala que fue insertado sin normalizar formato — dificulta legibilidad y diff. | baja |
| .bl-sprint-dropdown z-index               | Dropdown de sprint bar: z-index 120.                                                                     | z-index 120 — entre --z-sticky (100) y --z-sidebar (200). No usa el token --z-dropdown (400). Puede quedar bajo el sidebar en scroll.                  | media     |
| .bl-sprint-overlay                        | Overlay para cierre del dropdown: fixed inset, z-index 119.                                              | z-index 119 — debe estar bajo el dropdown (120) ✓, pero hardcodeado. Si el dropdown z-index cambia, el overlay puede quedar por encima.                | baja      |
| §15 CSS Purity — utilities                | `.hidden`, `.d-flex`, `.d-inline-block` declaradas con `!important`.                                     | 3 clases utility con `!important`. Correcto para utilities pero `!important` en `.d-flex` y `.d-inline-block` puede generar colisiones en componentes que necesiten override de display (ej. mobile hide/show). | media |
| .idp-ac-label                             | Label de sección AC en panel de detalle con `margin-bottom: 8px !important`.                             | `!important` en regla de espaciado interno de componente — no en utility. Señal de especificidad no resuelta en el componente. Fix correcto era aumentar especificidad, no usar !important. | media |
| .blind-exhaust-confirm-btn                | Botón de confirmación de agotamiento ciego: usa `var(--radius)`.                                         | `var(--radius)` referencia una variable no declarada en :root. El token correcto es `--radius-md` o `--radius-sm`. Fallback no especificado → valor initial (0) en caso de no resolverse. | alta |
| .blind-exhaust-cancel-btn                 | Botón cancelar en mismo bloque: también usa `var(--radius-pill)`.                                        | `var(--radius-pill)` sí está declarado ✓. Sin deuda.                                                                                                   | —         |
| R-[pendiente-ID] arch-zone-divider        | Separador visual zona de archivo histórico: borde top/bottom + height fijo.                              | `var(--surface2, rgba(0,0,0,0.04))` — el fallback rgba es un valor sin relación con el sistema de color del tema dark. En dark mostraría un gris oscuro, en light sería invisible. Fallback incorrecto. | media |

---

audit-ai-tracker-session.js-1f


| Función / Bloque                         | Qué hace realmente                                                                                                       | Deuda técnica detectada                                                                                                                                                                                                                                                      | Severidad |
|------------------------------------------|--------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| CANONICAL_PROJECTS (L4)                  | Tabla de strings canónicos validada en parsePaste()                                                                      | Contiene 'Obsidiana Labs' y 'Obsidiana' — ambos deprecados. String canónico actual es 'Obsidian Labs' (sin 'a'). La validación en parsePaste() aceptará el valor incorrecto. Comentario en L3 también declara el string deprecado.                                            | alta      |
| parseCheckpoint() — path JSON (L9–69)    | Detecta bloque ```json y parsea CHECKPOINT formato R-202605-133                                                          | _typedLines (L39–42) acepta campo `desc` como fallback de `title` — Base Rules §6 declara `desc` inválido desde versión actual. Introduce soporte silencioso a campo deprecado.                                                                                               | media     |
| parseCheckpoint() — path legacy (L71–152)| Extrae campos del bloque Markdown ---CHECKPOINT--- con regex                                                             | extractField (L84–88) solo captura la primera línea de un campo. Para campos multilinea (Bloqueantes, Decisión con texto largo) silencia el resto. Sin deuda mayor estructural; el comportamiento es consistente con su propósito de read-only.                               | baja      |
| _setPhase() (L156–164)                   | Actualiza barra de progreso 3 fases del card via IDs dinámicos                                                           | Ninguna deuda detectada.                                                                                                                                                                                                                                                     | —         |
| parsePaste() (L166–557)                  | Orchestrador principal: detecta tipo de input, delega parseo, valida Proyecto, construye preview, persiste draft          | (1) Usa window[`_itemsJsonError_${id}`] como canal de error entre ramas — estado global compartido via window[], no encapsulado. Riesgo de colisión entre IDs concurrentes o al reutilizar el mismo card. (2) path JSON (L186–230) y path legacy (L233–309) duplican íntegramente la lógica de validación de ítems (campos oblig., type, status, construcción de tgItems) — ~90 líneas de código idéntico. (3) Validación de proyecto (L473–479) es case-sensitive pero la pill de preview (L510) usa toLowerCase() para comparar — comportamiento inconsistente visible al usuario. (4) CSS Purity: L511–515 usa style= inline con CSS vars calculados en JS — violación de Base Rules §15 si las variables no están declaradas en CSS. (5) draft a Supabase (L352–359) no tiene manejo de error de red — solo QuotaExceededError de localStorage está cubierto. | alta      |
| handlePaste() (L562–597)                 | Difiere parsePaste() 150ms tras onpaste para esperar inserción del clipboard; reintenta a 300ms si ta.value vacío         | Timeout heurístico (150ms / 300ms) — frágil en browsers lentos o bajo carga. No hay mecanismo de cancelación si el card se destruye antes del timeout; puede ejecutar sobre DOM eliminado.                                                                                    | media     |
| handleInput() (L599–603)                 | Guard de _pasteInFlight antes de llamar parsePaste()                                                                     | Ninguna deuda detectada.                                                                                                                                                                                                                                                     | —         |
| _tryIngestPlan() (L606–649)              | Detecta y mergea bloques ---PLAN--- / ---EXECUTION-PLAN--- en localStorage                                               | Si incomingHasSesion && incomingHasSprint (ambos scopes), reemplaza completo sin confirmación al usuario — comportamiento destructivo silencioso documentado solo en comentario interno (L634).                                                                               | media     |
| parsePasteStandalone() (L659–789)        | Flujo CHECKPOINT sin IA: parsea, valida, construye preview para el modal standalone                                       | (1) Duplica validación de ítems completa (type, status, campos oblig., construcción de tgItems) — tercera copia del mismo bloque de ~35 líneas. (2) L710 tiene atributo class= duplicado en el mismo div: `class="paste-error" class="paste-error paste-warn"` — el segundo class es ignorado por el parser HTML.                                                    | media     |
| saveStandaloneCheckpoint() (L791–864)    | Aplica CHECKPOINT standalone al backlog del proyecto activo; delega a showMergeDiffPanel si disponible                   | L796–798: si no hay ítems devuelve toast y retorna — pero L788 ya deshabilitó el botón en ese caso; la guarda nunca llegaría aquí con 0 ítems. Lógica defensiva redundante pero no dañina.                                                                                  | baja      |
| parsePlanBlock() (L902–1004)             | Parser YAML-like de ---PLAN--- / ---EXECUTION-PLAN--- — produce array de sprints con sessions                            | (1) _parseList (L934–938) usa split(/[,\s]+/) — un nombre de archivo con espacios (ej: "mi archivo.js") se parte en tokens incorrectos. (2) El parser no valida que el terminador (---EXECUTION-PLAN-END--- / ---PLAN-END---) esté presente; si falta, match() devuelve null y la función devuelve null silenciosamente — sin error al caller. (3) isNew detectado por presencia de ---EXECUTION-PLAN--- en texto completo, no solo en el bloque — si un CHECKPOINT contiene el string como texto libre se activaría el path incorrecto. | media     |
| _horaUpdate() / parseHora() / correctHora() (L1007–1041) | Componente reutilizable de interpretación de hora — actualiza display según estado del input | Ninguna deuda detectada.                                                                                                                                                                                                                                                     | —         |
| interpretHora() (L1043–1060)             | Convierte string numérico HHMM en objeto {h, m, hhmm, label, epoch}                                                     | epoch siempre apunta a la próxima ocurrencia futura (L1056–1058) — si el usuario ingresa una hora pasada, el epoch queda en el día siguiente. Este comportamiento está comentado (B-202604-009) pero puede sorprender en flujos de corrección de hora de sesiones anteriores. | baja      |
| fmt12() (L1062–1068)                     | Convierte "HH:MM" a formato 12h con AM/PM                                                                                | No valida que hhmm sea un string con formato correcto — si recibe undefined o formato inesperado, split(':') produce NaN silencioso.                                                                                                                                          | baja      |
| relDate() (L1073–1123)                   | Fecha relativa — acepta timestamp numérico o string "3 May. 2026"                                                        | (1) Rama 2 (string): el mapeo `months` (L1099) no incluye todos los meses en todos los idiomas/locales — "May" en inglés no matchea 'may' en español si el sistema devuelve el mes en inglés. (2) diffDays <= 45 → "Hace 1 mes" (L1091/L1121) — 45 días puede ser 1 o 2 meses; umbral impreciso.                                                                         | baja      |
| _buildNarrativeMemoryMd() (L1249–1269)   | Genera sección Markdown con últimas 5 sesiones que tienen campos narrativos — para CONTEXT exportado global              | Itera sobre _getAllSessionsChron() que agrega sesiones de todos los proyectos — sin filtrar por proyecto activo. El CONTEXT exportado puede incluir memoria de proyectos distintos al activo.                                                                                  | media     |
| _doDownloadTemplates() (L1271–1307)      | Descarga Backlog.md, CONTEXT.md y HTML-MAP.md con setTimeout encadenados                                                 | (1) Descargas coordinadas con setTimeout(300ms) y setTimeout(600ms) — si el usuario tiene localStorage vacío o lento, las descargas fallan silenciosamente sin feedback. (2) Blob + URL.createObjectURL sin revokeObjectURL en el path de CONTEXT si raw está vacío — sin leak real porque la rama no ejecuta, pero el patrón es inconsistente con el path de MAP que sí revoca.                                                                                      | baja      |
| _addChangelogEntry() (L1313–1331)        | Extrae versión del campo archivos via regex y agrega entrada al changelog en localStorage                                 | Regex de versión (L1321) busca 'AI-Tracker-(v[\d.]+).html' — si el archivo se llama 'AI-Tracker-v3.5.1.html', captura 'v3.5.1'; si se llama de otra forma, cae back a APP_VERSION. Frágil ante cambios de naming de archivo. Sin deuda crítica.                              | baja      |
| buildContextMd() (L1408–1559)            | Genera texto completo del CONTEXT.md para exportar — incluye estado, stack, proyectos, gaps, decisiones, narrativa       | (1) stackSection (L1448) y tabsSection (L1467) y localStorageSection (L1470) son strings estáticos hardcodeados — no reflejan el estado real del sistema. Si el stack cambia, este export queda desactualizado sin aviso. (2) `buildContextMd` genera doble encabezado (L1518–1519): `# CONTEXT — AI Tracker` seguido de `# CONTEXT.md` — dos H1 en el mismo documento. | media     |
| buildBacklogMd() (L1562–1617)            | Genera Backlog.md para exportar desde items del tracker activo                                                           | (1) Itera sobre item.code[0] para clasificar por tipo (L1599) — si code está vacío o malformado, acceso a index 0 devuelve undefined y el ítem se ignora silenciosamente. (2) Usa item.desc en L1604 — campo deprecated según Base Rules §6; debería ser item.title. (3) Tipo 'I' incluido en byType (L1597) pero no existe en el sistema de numeración (Base Rules §5) — ítems con código I- nunca existirán.                                                          | media     |
| _checkStorageQuota() (L1620–1628)        | Muestra toast de advertencia si localStorage supera 70% o 85%                                                            | Depende de _getLocalStorageUsage() definida en otro módulo — sin guard de tipo robusto más allá del typeof check inicial. Sin deuda mayor.                                                                                                                                    | baja      |
| saveSession() (L1630–1687)               | Orquesta guardado: valida título, hora, proyecto, delega a _doSaveSession()                                               | (1) Doble declaración de variable `ta` (L1633 y L1640) — ta2 en L1640 es redundante; podría reutilizar ta. Cosmético pero indica copy-paste. (2) La lógica de fallback de título (L1636–1637) puede capturar la primera línea del body del CHECKPOINT si el título parseado está vacío — potencialmente guardando "Resumen:" como título.                             | media     |
| _mergeBacklogWithProject() (L1713–1734)  | Merge de ítems al proyecto del card, no al filtro global — sobrescribe temporalmente localStorage para apuntar al proyecto correcto | Sobrescritura temporal de localStorage (L1719–1730) con restauración en finally — si loadBacklog() o mergeBacklogFromTG() tienen efectos secundarios que persisten tras la restauración, el estado puede quedar inconsistente. Patrón de "hack de contexto" documentado en comentarios.                                                                             | media     |
| _doSaveSession() (L1737–1895)            | Segunda mitad del guardado: crea newSess, actualiza tracker legacy del proyecto, delega merge a showMergeDiffPanel o _doApplyMergeAndFinish | (1) Tracker legacy (L1869–1881): actualiza tracker.items del proyecto con campos limitados (desc, status, sessionId) — duplica datos ya en el backlog principal con schema distinto. Dos fuentes de verdad para el mismo ítem. (2) L1876: regex [PITRB] incluye 'I' — tipo inexistente en el sistema. (3) newSess creado (L1842) y push a sessions (L1861) antes de showMergeDiffPanel — si el usuario cancela el panel, la sesión queda guardada sin items mergeados.           | alta      |
| _doApplyMergeAndFinish() (L1898–2034)    | async — aplica merge, secciones CONTEXT/MAP, plan, limpia estado, renderiza                                               | (1) Declarada async pero no usa await excepto en saveImmediate() (L1944) — el flujo de merge y render es síncrono; la declaración async es innecesaria y puede enmascarar errores no capturados. (2) Doble render() dentro de requestAnimationFrame (L1960, L1812) — render ejecuta 2 veces por guardado, potencial de flash visual o race con estado intermedio. (3) mergeResult referenciado en L1982 dentro del rAF callback pero declarado fuera del scope del rAF en función hermana — funciona por closure pero crea dependencia de orden de ejecución implícita.           | media     |
| openDetail() / openPopup render (L2045–2267) | Renderiza popup / preview panel de sesión con todos sus campos, edición inline, badges, tracker items                  | (1) L2135: elemento tiene `class="btn-ghost" class="btn-unlock-now"` — doble atributo class, el segundo es ignorado. CSS de btn-unlock-now nunca aplica. (2) L2249: mismo patrón — `class="btn-ghost" class="btn-danger-sm"`. (3) Genera HTML inline extenso (topFields, midFields) directamente en strings — si esc() falla en un campo con valor null, genera "null" visible en UI. (4) tgItems redeclarada con const en L2150 dentro del mismo scope de la función donde parsed usa tgItems — shadowing del parámetro de _doSaveSession sin relación. | media     |
| _showProjRequiredInPanel() (L1138–1213)  | Muestra banner de proyecto requerido en el panel item-viz con selector inline                                             | Clona el botón de confirmación para limpiar handlers (L1169–1170) — patrón correcto pero si el overlay se cierra y reabre antes de que el listener de change ejecute, _resolvedProj puede quedar en null aunque el usuario seleccionó un proyecto.                            | baja      |
| _showProjMismatchModal() (L1690–1707)    | Modal Continuar/Cancelar para discrepancia de proyecto en CHECKPOINT                                                      | Misma técnica de cloneNode para limpiar handlers (L1700) — patrón correcto.                                                                                                                                                                                                  | —         |
| startPopupEdit() (L2665–2733)            | Edición inline de campos title/summary/pending en popup — reemplaza contenido del elemento con input/textarea temporal    | blur dispara commit con delay 150ms (L2731) — si render() es llamado entre blur y el setTimeout, openDetail() se ejecuta dos veces (una por commit, otra por el render). Flag `done` mitiga el doble commit pero no el doble render.                                         | baja      |
| startRename() (L2735–2767)               | Edición inline del nombre de IA en card                                                                                  | Escape cancela removiendo el listener blur y llamando cancel() — pero si blur ya disparó antes de Escape, commit se ejecuta igualmente. Race condition de bajo impacto.                                                                                                      | baja      |
| editNotes() / saveNotes() / renderNotesDisplay() (L2771–2834) | Notas libres por IA — edición inline con textarea en el card                               | wrap._ta (L2788) asigna referencia al DOM element en propiedad del elemento DOM padre — patrón no estándar. Si wrap es reemplazado por render() antes de saveNotes(), wrap._ta apunta a nodo huérfano.                                                                       | media     |
| _rebuildLogBody() (L3008–3093)           | Reconstruye el log card completo (header + filas) con filtros aplicados                                                   | (1) Registra event listener de scroll (L3089–3091) en cada llamada sin remover el anterior — acumulación de listeners en logBody con cada reconstrucción. (2) Usa innerHTML completo para reconstruir — cualquier scroll position se pierde en cada rebuild.                  | alta      |
| _getAllSessionsChron() (L2866–2876)       | Recopila todas las sesiones de todos los proyectos, ordenadas cronológicamente inversas                                   | parseInt(sess.id) para ordenar (L2874) — sess.id es Date.now().toString(), válido. Sin deuda.                                                                                                                                                                                | —         |
| _buildLogHeader() (L2911–2957)           | Construye HTML del header del log con pills de IA, tipo, proyecto y buscador                                              | color de IA (L2918): `${color}` interpolado directamente en atributo HTML sin nombre de atributo — genera HTML malformado: `<button class="..." #ff0000 ...>`. El atributo color sin nombre no aplica y genera warning en el parser.                                          | alta      |
| _buildLogRow() (L2961–3004)              | Construye HTML de una fila del log                                                                                        | _sessRelTsShared (L2983) referenciada con typeof guard — dependencia externa no documentada. Si no está disponible, cae back a dateShort silenciosamente.                                                                                                                     | baja      |
| window load hook — monkey-patching (L3179–3233) | Parchea render() y navigateToCard() globales + registra listeners ESC y click-fuera para cerrar log card           | (1) Monkey-patch de render() global (L3185–3188): si render() es redefinido después de este load, el patch se pierde. (2) Click-outside (L3211–3226): usa document.contains(e.target) para detectar nodos removidos — correcto, pero el log-scroll-top listener (L3089) puede acumularse separado de este flujo sin cleanup.                                            | media     |
| scrollToLogCard() (L3100–3123)           | Navega al log card y opcionalmente resalta una fila                                                                       | _rebuildLogBody() dentro de requestAnimationFrame-adjacent — si currentTab no es 'tracker', switchTab() es asíncrona con su propio rAF; la referencia a card puede ser stale.                                                                                                | baja      |
| navigateToBacklogItem() (L3236–3248)     | Navega a un ítem del backlog por código                                                                                   | setTimeout de 120ms para esperar render del tab (L3241) — mismo patrón heurístico frágil de handlePaste(). Si el render tarda más de 120ms, scrollIntoView() no encuentra el elemento.                                                                                      | baja      |

---

audit-index.html-1g

| Función / Bloque                                      | Qué hace realmente                                                                                        | Deuda técnica detectada                                                                                                                                                                                                     | Severidad |
|-------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| HEAD — carga de dependencias                          | Carga env.js, Supabase SDK, JSZip y hojas CSS                                                             | env.js y Supabase SDK declarados sin defer/async — bloqueantes de renderizado. JSZip usa defer, los demás no. Estrategia de carga inconsistente entre dependencias.                                                         | media     |
| <html lang="es" data-theme="light">                   | Declara idioma y tema inicial                                                                             | data-theme="light" hardcodeado en el atributo HTML. Si el usuario tiene dark guardado en localStorage, patchApplyTheme lo corrige tarde en DOMContentLoaded — flash of incorrect theme garantizado en cada carga.            | media     |
| Inline script block 1 (toggleHeaderSearch et al.)     | Controla búsqueda animada, badge de backup, init de command palette y trigger de resumen semanal          | Lógica de aplicación en bloque <script> inline dentro del HTML. No vive en ningún archivo .js del ecosistema. Viola separación de capas. Incluye referencias a IDs inexistentes: #backup-badge y #version-pill no están en el DOM. | alta      |
| Inline script block 2 (patchApplyTheme IIFE)          | Monkey-patch de applyTheme para forzar setAttribute en <html>                                             | Parche inline que corrige un contrato roto en el .js externo. La función original no cumple su responsabilidad — la corrección debería vivir en la fuente, no en el HTML.                                                   | alta      |
| Tab Tracker — #tracker-sidebar (legacy)               | Preserva IDs tsb-insession / tsb-available / tsb-exhausted para backward compat                           | DOM vacío mantenido por acoplamiento oculto con JS. Tres contenedores que nunca renderizan contenido visible. Deuda estructural de una refactorización incompleta (R-202604-036).                                            | media     |
| Tab Backlog — #toolbar (legacy)                       | Declarada "mantenida por compatibilidad con JS que la referencia"                                         | style="display:none" inline en elemento estático — viola CSS Purity §15. Dead DOM con coupling a JS no trazable desde HTML. Dos puntos de deuda simultáneos.                                                               | alta      |
| Tab Backlog — #btn-import-backlog                     | Botón de importación de backlog deshabilitado permanentemente                                             | style="display:none" inline en elemento estático — viola CSS Purity §15. El botón sigue en DOM activo pese a comentario que declara el feature como deprecado. Estado de visibilidad no controlado por clase CSS.           | alta      |
| Tab Backlog — #item-type select (item editor)         | Selector de tipo de ítem en formulario de creación                                                        | Opción duplicada para "Idea": value="I" y value="P" con el mismo label. Base Rules §5 define solo "P" como tipo Idea válido. El tipo "I" no existe en el sistema de numeración.                                             | media     |
| Reset backlog modal (#reset-backlog-overlay)          | Confirmación de reset total del backlog con verificación de texto                                         | Lógica de validación (comparación de string, toggle de clase, texto de error) embebida en atributo oninput inline. Lógica de aplicación no trivial en HTML. Viola separación HTML/JS §17.                                  | alta      |
| #cp-overlay — Command Palette (T-202604-419)          | Command palette con búsqueda, recientes y resultados; sus estilos viven en el bloque <style> de T-202605-442 | Implementación completa duplicada de command palette. Coexiste en DOM con #cmd-palette-overlay (T-202604-418). Dos shells activos del mismo concepto con IDs, estilos y lógica distintos. Uno es dead code estructural.     | alta      |
| #cmd-palette-overlay — Command Palette (T-202604-418) | Segunda implementación de command palette con input, lista y footer de hints                              | Duplicado de #cp-overlay. No es posible determinar desde el HTML cuál es la implementación activa. Ambos están en DOM sin condición de exclusión visible.                                                                   | alta      |
| Inline <style> blocks (×4)                            | Estilos para shortcuts/command palette, tracker views, cronómetro/weekly modal, user chip                 | Cuatro bloques <style> embebidos en HTML en lugar de archivos .css. Uno tiene comentario explícito "migrar a ai-tracker-extra.css" (T-202604-423). Viola CSS Purity §15. Los estilos del ecosistema deben vivir en .css.   | alta      |
| #global-footer — gf-pulso span                        | Indicador visual del Pulso del Ecosistema con click handler                                               | style="cursor:pointer" inline en elemento estático — viola CSS Purity §15.                                                                                                                                                  | alta      |
| Panel de proyectos #proj-panel (T-077)                | Panel lateral para ver y navegar proyectos                                                                | Botones "⚙ Gestionar" y "+ Nuevo proyecto" llaman al handler idéntico: closeProjPanel();openProjModal(). Labels diferentes, comportamiento indistinguible.                                                                  | baja      |
| Comentarios de tickets done/deprecados                | Referencias históricas a trabajo completado o removido                                                    | ≥5 comentarios sin valor activo: DEPRECATED T-202604-254, T-202605-491 (Firebase wizard removido), T-202604-423 (estilos migrados), R-202604-086 (estilos migrados), T-202604-324 (mini progress dots sin DOM asociado). Ruido de lectura que oscurece el estado real del archivo. | baja      |

---

audit-ai-tracker-command-palette.js + ai-tracker-map-generator.js + ai-tracker-sprint-project.js + env.js-1h

| Función / Bloque                          | Qué hace realmente                                                                                                         | Deuda técnica detectada                                                                                                                                                                           | Severidad |
|-------------------------------------------|----------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| _getAllBacklogItems (CP)                  | Lee backlog de localStorage por proyecto activo                                                                            | Si getActiveProject() devuelve null, usa clave genérica 'backlog-items' — inconsistente con _tplKey() que usan otros módulos. Puede leer el backlog incorrecto.                                  | alta      |
| _cpSearchContext (CP)                     | Busca texto en el raw del CONTEXT del proyecto activo                                                                      | Usa 'context-raw' como key en localStorage; el resto del app usa getProjContext(). Dos fuentes de verdad distintas para el mismo dato. Puede devolver contexto de proyecto equivocado.           | alta      |
| action-search-context comando (CP)        | Navega al sub-tab contexto                                                                                                 | Llama switchTab('backlog') sin prefijo 'tab-' — inconsistente con todos los demás comandos que usan 'tab-backlog'. Si switchTab valida el ID exacto, este comando falla silenciosamente.         | alta      |
| _buildDynamicCommands — proyectos (CP)    | Genera comandos dinámicos por proyecto activo                                                                              | Accede a window.state.projects directamente, y también comprueba getActiveProject(). Doble punto de acceso al mismo estado sin garantía de consistencia.                                         | media     |
| _cpOnListClick (CP)                       | Ejecuta comando al hacer click en resultado                                                                                | Usa parseInt(item.dataset.idx) para indexar _cp.results. Si los resultados cambian entre mouseover y click (búsqueda async futura), el índice puede quedar desincronizado.                      | media     |
| _buildCommandRegistry — action-new-item (CP) | Abre editor de ítem nuevo                                                                                               | setTimeout de 150ms hardcodeado para esperar el cambio de tab antes de abrir el editor. Frágil ante cambios de velocidad de render.                                                              | baja      |
| action-use-template (CP)                  | Navega a backlog y abre template picker                                                                                    | Doble setTimeout anidado (150ms + 120ms) para orquestar navegación + apertura de modal. Race condition potencial si switchTab o openItemEditor son lentos.                                       | baja      |
| _cp.registry (CP)                         | Almacena registro de comandos estáticos                                                                                    | Se regenera en cada apertura con _buildCommandRegistry(). Si el registro es grande, cada apertura tiene costo de reconstrucción. No hay cache entre aperturas.                                   | baja      |
| _mgBuildPlan — emisión de bloque (MG)     | Genera bloque ---PLAN--- (formato legacy)                                                                                  | Emite ---PLAN--- / ---PLAN-END--- en lugar de ---EXECUTION-PLAN--- / ---EXECUTION-PLAN-END--- que define Base Rules §9a. El bloque generado es incompatible con el parser activo de PP.          | alta      |
| _generateContext — stack rewrite (MG)     | Reescribe referencias a Firebase en el stack por Supabase                                                                  | Regex en texto libre puede hacer match en nombres de variables o comentarios legítimos que contengan "Firebase Firestore". Sin test de cobertura visible.                                        | media     |
| _generateContext — commands (MG)          | Construye mapa de arranque por rol desde getAISessions()                                                                   | getAISessions() sin definición visible en este archivo — dependencia implícita de función global. Si falla, el fallback es un placeholder explícito, pero el error se silencia en catch.         | media     |
| _mgInferStatus (MG)                       | Infiere estado operativo del proyecto (closing/active/planning/between_sprints)                                            | Detecta 'closing' comprobando visibilidad del modal #close-sprint-modal por clase, style.display o aria-hidden. Los tres checks son inconsistentes entre sí — uno puede ser true cuando otro es false. | media |
| _mgLoadFiles — deduplicación (MG)         | Carga archivos al dropzone                                                                                                 | Deduplica por nombre exacto únicamente. Si el usuario sube una versión actualizada del mismo archivo, la versión anterior persiste sin warning.                                                   | media     |
| generateDocuments — validación CONTEXT (MG) | Valida campos obligatorios antes de generar                                                                             | Calcula _blItems dos veces en el mismo bloque (AC3) con código idéntico duplicado: una para _valStatus, otra para _valTotal/_valPending. Duplicación sin extracción.                            | baja      |
| _mgExportAllZip (MG)                      | Exporta todos los documentos en ZIP                                                                                        | Referencia a exportFullHistoryMd() está comentada con "descarga directamente — la llamamos en fallback", pero el fallback sí la llama. El comentario contradice el código.                      | baja      |
| confirmMapGenerator — guard sprint cerrado (MG) | Bloquea bump de versión si no hay sprint cerrado                                                                    | showToast de warning se muestra y la función retorna sin descargar nada ni cerrar el modal. El usuario no recibe instrucción clara de qué acción tomar.                                         | baja      |
| _mgApplyBumpedVersion (MG)                | Persiste versión bumpeada y actualiza DOM                                                                                  | El comentario declara "Fuente de verdad única: localStorage key 'app-version-override' declarada en ai-tracker-checkpoint.js". Esta función la escribe directamente con string literal en lugar de usar la constante — acoplamiento implícito entre módulos.  | media |
| _mgNow (MG)                               | Genera timestamp legible                                                                                                   | Usa toLocaleString('es-MX') — el resultado depende del locale del navegador del usuario. En navegadores sin soporte completo de es-MX, el formato puede variar.                                  | baja      |
| _docPrefix (SP)                           | Retorna prefijo del proyecto activo                                                                                        | _PREFIX_MAP contiene 'Obsidiana': 'OB' — hardcodeado con nombre y prefijo legacy. OL-CONTEXT §19 declara que el prefijo fue actualizado a OL. El mapa no refleja el cambio.                     | alta      |
| _generateBacklogMd — filtro generacional (SP) | Filtra ítems para export según sprint activo/cerrado                                                                  | Accede a state.sprints directamente en lugar de getActiveSprints(). En otros puntos del mismo archivo se usa getActiveSprints(). Inconsistencia de acceso al estado de sprints.                  | media     |
| _generateBacklogMd — contadores (SP)      | Calcula contadores máximos de IDs por tipo                                                                                 | ITEMS[i].code[0] asumido siempre válido — si code es null/undefined, code[0] lanza TypeError. No hay guard.                                                                                      | media     |
| _buildCurrentStateMd (SP)                 | Genera bloque ## Estado actual para el backlog exportado                                                                   | Accede a state.sprints directamente. state.sprints y getActiveSprints() pueden devolver datos distintos si hay transformación intermedia. Mismo problema que _generateBacklogMd.                  | media     |
| _generateSprintsExportMd — _itemRow (SP)  | Genera fila de ítem en tabla de sprint                                                                                     | i.code[0] sin guard — mismo riesgo de TypeError que _generateBacklogMd si code es null.                                                                                                          | media     |
| _generateFullHistoryBySprintMd (SP)       | Genera historial completo agrupado por sprint                                                                              | SPRINT_DATA_THRESHOLD hardcodeado en 23 como constante local — no hay campo de configuración ni derivación desde datos de sprints. Cambiar el umbral requiere edición de código.                  | baja      |
| selectProjectFilter / clearProjectFilter (SP) | Cambia filtro activo de proyecto y re-renderiza                                                                       | Ambas funciones llaman la misma secuencia de 7 funciones de render. El bloque no está extraído en una función reutilizable. Duplicación de lógica de refresh.                                    | baja      |
| _renderProjList — archived toggle (SP)    | Toggle de sección archivados en modal de proyectos                                                                         | El handler del toggle usa un string JavaScript inline en onclick con lógica de localStorage. Viola la separación de lógica del HTML y es inauditable desde el código.                             | baja      |
| SplashController.updateProgress (SP)      | Actualiza barra de progreso del splash                                                                                     | Usa style.setProperty('--splash-progress', percent + '%') — CSS custom property. Correcto. Sin deuda.                                                                                           | —         |
| cleanupLocalStorage / testLocalStorageQuota (SP) | Herramientas de debug expuestas en window                                                                          | Funciones de debug expuestas globalmente en producción sin flag de entorno. cleanupLocalStorage() puede ser invocada accidentalmente desde consola por el usuario.                                | baja      |
| Keyboard shortcut Ctrl+K (SP y CP)        | Abre búsqueda global o command palette                                                                                     | Dos listeners de keydown para Ctrl+K en el mismo documento: uno en ai-tracker-sprint-project.js (abre #search-global) y otro en ai-tracker-command-palette.js (abre command palette). Conflict de handlers — el que se registra último gana, o ambos disparan. | alta |
| env.js                                    | Expone SUPABASE_URL y SUPABASE_ANON_KEY en window.__ENV                                                                   | anon key expuesta en texto plano en archivo JS servido al cliente. Esperado para Supabase anon key (solo acceso público), pero el archivo no tiene ningún comentario que documente esto. Sin deuda funcional; riesgo de confusión de seguridad para mantenedores. | baja |

---

audit-MAP-1i

{
  "version": "v3.5",
  "updated": "2026-05-07 12:00 UTC-6",
  "project": "AI",
  "files": [
    {
      "name": "ai-tracker-backlog.js",
      "type": "js",
      "lines": 7866,
      "functions": [
        { "line": "L76", "name": "renderHtmlMap", "area": "Rendering" },
        { "line": "L252", "name": "ITEMS (IIFE init)", "area": "Storage" },
        { "line": "L466", "name": "_calcPriority", "area": "Internal" },
        { "line": "L490", "name": "_calcRelevanceScore", "area": "Internal" },
        { "line": "L552", "name": "_recalcAllScores", "area": "Internal" },
        { "line": "L563", "name": "_sanitizePendingInClosedSprints", "area": "Internal" },
        { "line": "L623", "name": "_purgeStaleBacklogCache", "area": "Storage" },
        { "line": "L643", "name": "loadBacklog", "area": "Storage" },
        { "line": "L805", "name": "_getNextItemCode", "area": "Internal" },
        { "line": "L826", "name": "parseBacklogMd", "area": "Internal" },
        { "line": "L962", "name": "importBacklog", "area": "Storage" },
        { "line": "L1105", "name": "setItemStatus", "area": "Internal" },
        { "line": "L1191", "name": "_resetStatusSelect", "area": "UI" },
        { "line": "L2172", "name": "_buildOption (in _buildSprintSelector)", "area": "UI" },
        { "line": "L2236", "name": "_buildOption (in _blSprintOpen)", "area": "UI" },
        { "line": "L3624", "name": "_attachBacklogDnD", "area": "UI" },
        { "line": "L3714", "name": "_buildChildrenBlock", "area": "Rendering" },
        { "line": "L3873", "name": "buildBacklogItem", "area": "Rendering" },
        { "line": "L4567", "name": "mergeBacklogFromTG", "area": "Internal" },
        { "line": "L4823", "name": "showMergeDiffPanel", "area": "UI" },
        { "line": "L6412", "name": "_scmRender", "area": "Rendering" },
        { "line": "L6461", "name": "_scmStep1Html", "area": "Rendering" },
        { "line": "L7794", "name": "_patchMoreMenuReset (IIFE)", "area": "Internal" },
        { "line": "L7832", "name": "_initFocusShortcut (IIFE)", "area": "Internal" }
      ]
    },
    {
      "name": "ai-tracker-checkpoint.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L55", "name": "_updateHeaderProjectLabel", "area": "UI" },
        { "line": "L124", "name": "Supabase init block", "area": "Storage" },
        { "line": "L166", "name": "getSession (post-listener)", "area": "Storage" },
        { "line": "L453", "name": "switchTab", "area": "Navigation" },
        { "line": "L466", "name": "_calcPriority", "area": "Internal" },
        { "line": "L490", "name": "_calcRelevanceScore", "area": "Internal" },
        { "line": "L563", "name": "_sanitizePendingInClosedSprints", "area": "Internal" },
        { "line": "L623", "name": "_purgeStaleBacklogCache", "area": "Storage" },
        { "line": "L643", "name": "loadBacklog", "area": "Storage" },
        { "line": "L805", "name": "_getNextItemCode", "area": "Internal" },
        { "line": "L826", "name": "parseBacklogMd", "area": "Internal" },
        { "line": "L962", "name": "importBacklog", "area": "Storage" },
        { "line": "L1105", "name": "setItemStatus", "area": "Internal" },
        { "line": "L1191", "name": "_resetStatusSelect", "area": "UI" },
        { "line": "L2172", "name": "_buildOption (in _buildSprintSelector)", "area": "UI" },
        { "line": "L3624", "name": "_attachBacklogDnD", "area": "UI" },
        { "line": "L3714", "name": "_buildChildrenBlock", "area": "Rendering" },
        { "line": "L3873", "name": "buildBacklogItem", "area": "Rendering" },
        { "line": "L4567", "name": "mergeBacklogFromTG", "area": "Internal" },
        { "line": "L4823", "name": "showMergeDiffPanel", "area": "UI" },
        { "line": "L6412", "name": "_scmRender", "area": "Rendering" },
        { "line": "L6461", "name": "_scmStep1Html", "area": "Rendering" },
        { "line": "L7794", "name": "_patchMoreMenuReset (IIFE)", "area": "Internal" },
        { "line": "L7832", "name": "_initFocusShortcut (IIFE)", "area": "Internal" },
        { "line": "L1", "name": "_effectiveVersion", "area": "Internal" },
        { "line": "L1", "name": "_hasStaleSuggestion", "area": "Internal" },
        { "line": "L1", "name": "AVATAR_LOGOS", "area": "Config" },
        { "line": "L1", "name": "setSyncStatus", "area": "UI" },
        { "line": "L1", "name": "handleSyncPillClick", "area": "UI" },
        { "line": "L1", "name": "_offlineQueuePush", "area": "Storage" },
        { "line": "L1", "name": "_offlineQueueFlush", "area": "Storage" },
        { "line": "L1", "name": "_refreshMigrationBtnVisibility", "area": "UI" },
        { "line": "L1", "name": "signInWithSupabase", "area": "Storage" },
        { "line": "L1", "name": "signOutSupabase", "area": "Storage" },
        { "line": "L1", "name": "openAuthModal", "area": "UI" },
        { "line": "L1", "name": "closeAuthModal", "area": "UI" },
        { "line": "L1", "name": "signInWithMagicLink", "area": "Storage" },
        { "line": "L1", "name": "_scrollToCard", "area": "Navigation" },
        { "line": "L1", "name": "esc", "area": "Internal" },
        { "line": "L1", "name": "_loadTmpIdMap", "area": "Storage" },
        { "line": "L1", "name": "_saveTmpIdMap", "area": "Storage" },
        { "line": "L1", "name": "toggleRadarSidebar", "area": "UI" },
        { "line": "L1", "name": "_initRadarSidebarState", "area": "UI" },
        { "line": "L1", "name": "buildCard", "area": "Rendering" },
        { "line": "L1", "name": "render", "area": "Rendering" },
        { "line": "L1", "name": "_buildSessRow", "area": "Rendering" },
        { "line": "L1", "name": "avgBetweenSessions", "area": "Internal" },
        { "line": "L1", "name": "buildHoyCard", "area": "Rendering" },
        { "line": "L1", "name": "_cpBacklogItems", "area": "Command" },
        { "line": "L1", "name": "_cpRender", "area": "Command" },
        { "line": "L1", "name": "_cpSessionCommands", "area": "Command" },
        { "line": "L1", "name": "_saveUserPrefs", "area": "Storage" },
        { "line": "L1", "name": "normStatus", "area": "Internal" },
        { "line": "L1", "name": "buildTGPreview", "area": "Rendering" },
        { "line": "L1", "name": "showMergeDiffPanel (checkpoint)", "area": "UI" },
        { "line": "L1", "name": "_showArranquePanel", "area": "UI" },
        { "line": "L1", "name": "_calcPulsoDotState", "area": "Internal" },
        { "line": "L1", "name": "openPulsoPanel", "area": "UI" },
        { "line": "L1", "name": "_trackerHistDayRender", "area": "Rendering" },
        { "line": "L1", "name": "_trackerRenderMiniHist", "area": "Rendering" },
        { "line": "L1", "name": "_trackerRenderHist", "area": "Rendering" },
        { "line": "L1", "name": "_trackerHistDragStart", "area": "UI" },
        { "line": "L1", "name": "_trackerHistDragEnd", "area": "UI" },
        { "line": "L1", "name": "_trackerHistAttachDropTargets", "area": "UI" }
      ]
    },
    {
      "name": "ai-tracker-ai-notes.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "toggleNotes", "area": "UI" },
        { "line": "L1", "name": "openAvatarModal", "area": "UI" },
        { "line": "L1", "name": "selectAvatarOption", "area": "UI" },
        { "line": "L1", "name": "confirmAvatarModal", "area": "UI" },
        { "line": "L1", "name": "closeAvatarModal", "area": "UI" },
        { "line": "L1", "name": "openAddAI", "area": "UI" },
        { "line": "L1", "name": "confirmAddAI", "area": "Internal" },
        { "line": "L1", "name": "confirmClear", "area": "UI" },
        { "line": "L1", "name": "deleteAI", "area": "Internal" },
        { "line": "L1", "name": "downloadReport", "area": "Internal" },
        { "line": "L1", "name": "downloadGlobalReport", "area": "Internal" },
        { "line": "L1", "name": "toggleCardMenu", "area": "UI" },
        { "line": "L1", "name": "closeCardMenu", "area": "UI" },
        { "line": "L218", "name": "document.addEventListener (click global #1)", "area": "UI" },
        { "line": "L1", "name": "archiveAI", "area": "Internal" },
        { "line": "L1", "name": "toggleArchivedSection", "area": "UI" },
        { "line": "L1", "name": "showInlineConfirm", "area": "UI" },
        { "line": "L1", "name": "closeInlineConfirm", "area": "UI" },
        { "line": "L1", "name": "extractContextSections", "area": "Internal" },
        { "line": "L1", "name": "mergeContextSections", "area": "Internal" },
        { "line": "L1", "name": "extractHtmlMapSections", "area": "Internal" },
        { "line": "L1", "name": "mergeHtmlMapSections", "area": "Internal" },
        { "line": "L1", "name": "renderContext", "area": "Rendering" },
        { "line": "L1", "name": "renderContextMd", "area": "Rendering" },
        { "line": "L1", "name": "renderContextInline", "area": "Rendering" },
        { "line": "L1", "name": "savePlan", "area": "Storage" },
        { "line": "L1", "name": "loadPlan", "area": "Storage" },
        { "line": "L1", "name": "renderPlan", "area": "Rendering" },
        { "line": "L1", "name": "_buildPulsoPlanesHtml", "area": "Rendering" },
        { "line": "L1", "name": "_ctrKey", "area": "Storage" },
        { "line": "L1", "name": "_ctrLoad", "area": "Storage" },
        { "line": "L1", "name": "_ctrSave", "area": "Storage" },
        { "line": "L1", "name": "_ctrMergeFromItem", "area": "Internal" },
        { "line": "L1", "name": "_ctrUpdateBadge", "area": "UI" },
        { "line": "L1", "name": "onContratosSearch", "area": "UI" },
        { "line": "L1", "name": "clearContratosSearch", "area": "UI" },
        { "line": "L1", "name": "_ctrIsRisk", "area": "Internal" },
        { "line": "L1", "name": "renderContratos", "area": "Rendering" },
        { "line": "L1", "name": "openContratoDetail", "area": "UI" },
        { "line": "L1", "name": "_esc", "area": "Internal" },
        { "line": "L1", "name": "_renderContratoDetail", "area": "Rendering" },
        { "line": "L1", "name": "exportContratosMd", "area": "Internal" },
        { "line": "L1", "name": "resetContratosData", "area": "Storage" },
        { "line": "L1", "name": "searchContratos", "area": "Internal" }
      ]
    },
    {
      "name": "ai-tracker-extra.css",
      "type": "css",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": ":root dark/light design tokens", "area": "Config" },
        { "line": "L122", "name": "body global typography", "area": "UI" },
        { "line": "L255", "name": ".btn-primary", "area": "UI" },
        { "line": "L309", "name": ".sync-pill (base)", "area": "UI" },
        { "line": "L662", "name": "popup-overlay backdrop-filter", "area": "UI" },
        { "line": "L2286", "name": "responsive breakpoints (601px+)", "area": "UI" },
        { "line": "L3404", "name": ".sync-pill (states)", "area": "UI" },
        { "line": "L3717", "name": "quick-note-overlay backdrop-filter", "area": "UI" },
        { "line": "L9759", "name": "overlay generic backdrop-filter", "area": "UI" },
        { "line": "L12031", "name": "responsive breakpoint 900px", "area": "UI" },
        { "line": "L12760", "name": "responsive breakpoint 899px", "area": "UI" },
        { "line": "L13892", "name": "toolbar backlog redesign [tmp:toolbar-backlog-redesign]", "area": "UI" },
        { "line": "L17134", "name": "macOS Fidelity block", "area": "UI" },
        { "line": "L17947", "name": "export strip vars", "area": "UI" },
        { "line": "L18115", "name": ".mdiff-card type tokens", "area": "UI" },
        { "line": "L18353", "name": "toast-quota action buttons", "area": "UI" }
      ]
    },
    {
      "name": "ai-tracker.css",
      "type": "css",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "main stylesheet — core layout and component styles", "area": "UI" }
      ]
    },
    {
      "name": "ai-tracker-session.js",
      "type": "js",
      "lines": 3248,
      "functions": [
        { "line": "L4", "name": "CANONICAL_PROJECTS", "area": "Config" },
        { "line": "L9", "name": "parseCheckpoint (JSON path)", "area": "Internal" },
        { "line": "L71", "name": "parseCheckpoint (legacy path)", "area": "Internal" },
        { "line": "L156", "name": "_setPhase", "area": "UI" },
        { "line": "L166", "name": "parsePaste", "area": "Internal" },
        { "line": "L562", "name": "handlePaste", "area": "UI" },
        { "line": "L599", "name": "handleInput", "area": "UI" },
        { "line": "L606", "name": "_tryIngestPlan", "area": "Internal" },
        { "line": "L659", "name": "parsePasteStandalone", "area": "Internal" },
        { "line": "L791", "name": "saveStandaloneCheckpoint", "area": "Storage" },
        { "line": "L902", "name": "parsePlanBlock", "area": "Internal" },
        { "line": "L1007", "name": "_horaUpdate", "area": "UI" },
        { "line": "L1007", "name": "parseHora", "area": "Internal" },
        { "line": "L1007", "name": "correctHora", "area": "Internal" },
        { "line": "L1043", "name": "interpretHora", "area": "Internal" },
        { "line": "L1062", "name": "fmt12", "area": "Internal" },
        { "line": "L1138", "name": "_showProjRequiredInPanel", "area": "UI" },
        { "line": "L1690", "name": "_showProjMismatchModal", "area": "UI" },
        { "line": "L1737", "name": "_doSaveSession", "area": "Storage" },
        { "line": "L1898", "name": "_doApplyMergeAndFinish", "area": "Internal" },
        { "line": "L2045", "name": "openDetail", "area": "UI" },
        { "line": "L2665", "name": "startPopupEdit", "area": "UI" },
        { "line": "L2735", "name": "startRename", "area": "UI" },
        { "line": "L2771", "name": "editNotes", "area": "UI" },
        { "line": "L2771", "name": "saveNotes", "area": "Storage" },
        { "line": "L2771", "name": "renderNotesDisplay", "area": "Rendering" },
        { "line": "L2866", "name": "_getAllSessionsChron", "area": "Internal" },
        { "line": "L2911", "name": "_buildLogHeader", "area": "Rendering" },
        { "line": "L2961", "name": "_buildLogRow", "area": "Rendering" },
        { "line": "L3008", "name": "_rebuildLogBody", "area": "Rendering" },
        { "line": "L3100", "name": "scrollToLogCard", "area": "Navigation" },
        { "line": "L3179", "name": "window load hook (monkey-patching)", "area": "Internal" },
        { "line": "L3236", "name": "navigateToBacklogItem", "area": "Navigation" }
      ]
    },
    {
      "name": "index.html",
      "type": "html",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "HEAD dependency loading", "area": "Config" },
        { "line": "L1", "name": "inline script block 1 (toggleHeaderSearch et al.)", "area": "UI" },
        { "line": "L1", "name": "inline script block 2 (patchApplyTheme IIFE)", "area": "UI" },
        { "line": "L1", "name": "#tracker-sidebar (legacy tsb containers)", "area": "UI" },
        { "line": "L1", "name": "#toolbar (legacy, display:none)", "area": "UI" },
        { "line": "L1", "name": "#btn-import-backlog (deprecated, display:none)", "area": "UI" },
        { "line": "L1", "name": "#item-type select (item editor)", "area": "UI" },
        { "line": "L1", "name": "#reset-backlog-overlay", "area": "UI" },
        { "line": "L1", "name": "#cp-overlay (Command Palette T-202604-419)", "area": "Command" },
        { "line": "L1", "name": "#cmd-palette-overlay (Command Palette T-202604-418)", "area": "Command" },
        { "line": "L1", "name": "inline style blocks x4", "area": "UI" },
        { "line": "L1", "name": "#global-footer gf-pulso span", "area": "UI" },
        { "line": "L1", "name": "#proj-panel (T-077)", "area": "UI" }
      ]
    },
    {
      "name": "ai-tracker-command-palette.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "_getAllBacklogItems", "area": "Internal" },
        { "line": "L1", "name": "_cpSearchContext", "area": "Command" },
        { "line": "L1", "name": "action-search-context command", "area": "Command" },
        { "line": "L1", "name": "_buildDynamicCommands", "area": "Command" },
        { "line": "L1", "name": "_cpOnListClick", "area": "Command" },
        { "line": "L1", "name": "_buildCommandRegistry (action-new-item)", "area": "Command" },
        { "line": "L1", "name": "action-use-template", "area": "Command" },
        { "line": "L1", "name": "_cp.registry", "area": "Command" }
      ]
    },
    {
      "name": "ai-tracker-map-generator.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "_mgBuildPlan", "area": "Map" },
        { "line": "L1", "name": "_generateContext", "area": "Map" },
        { "line": "L1", "name": "_mgInferStatus", "area": "Map" },
        { "line": "L1", "name": "_mgLoadFiles", "area": "Map" },
        { "line": "L1", "name": "generateDocuments", "area": "Map" },
        { "line": "L1", "name": "_mgExportAllZip", "area": "Map" },
        { "line": "L1", "name": "confirmMapGenerator", "area": "Map" },
        { "line": "L1", "name": "_mgApplyBumpedVersion", "area": "Map" },
        { "line": "L1", "name": "_mgNow", "area": "Internal" }
      ]
    },
    {
      "name": "ai-tracker-sprint-project.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "_docPrefix", "area": "Sprint" },
        { "line": "L1", "name": "_generateBacklogMd", "area": "Sprint" },
        { "line": "L1", "name": "_buildCurrentStateMd", "area": "Sprint" },
        { "line": "L1", "name": "_generateSprintsExportMd", "area": "Sprint" },
        { "line": "L1", "name": "_generateFullHistoryBySprintMd", "area": "Sprint" },
        { "line": "L1", "name": "selectProjectFilter", "area": "Sprint" },
        { "line": "L1", "name": "clearProjectFilter", "area": "Sprint" },
        { "line": "L1", "name": "_renderProjList", "area": "UI" },
        { "line": "L1", "name": "SplashController.updateProgress", "area": "UI" },
        { "line": "L1", "name": "cleanupLocalStorage", "area": "Storage" },
        { "line": "L1", "name": "testLocalStorageQuota", "area": "Storage" }
      ]
    },
    {
      "name": "env.js",
      "type": "js",
      "lines": 0,
      "functions": [
        { "line": "L1", "name": "window.__ENV (SUPABASE_URL + SUPABASE_ANON_KEY)", "area": "Config" }
      ]
    }
  ]
}

---